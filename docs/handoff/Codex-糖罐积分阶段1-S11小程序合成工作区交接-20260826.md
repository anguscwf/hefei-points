# 糖罐积分阶段 1 / S11 小程序受控 synthetic 工作区交接（2026-08-26）

## 当前基线

- canonical 仓库：`C:\Users\ANGUS\projects\hefei-points`
- 当前分支：`codex/stage1-s11-miniapp-synthetic-profile-20260826`
- 基于 S10 文档提交：`e895c83 docs(stage1): record local privacy safety shell`
- 跟踪扫描修复：`f38b443 fix(miniapp): scan only audited tracked sources`
- 网络出口修复：`70a0826 fix(miniapp): constrain audited network routes`
- S11 功能提交：`2a3ace9 feat(miniapp): prepare controlled synthetic workspace`
- 文档提交：本交接所在的 `docs(stage1): record controlled miniapp synthetic workspace` 提交
- 只创建本地分支和本地提交；未 push、未部署、未修改远端 refs，未启动微信开发者工具，未 preview/upload，未执行 HAP/APP 打包、签名或 AppGallery 操作。
- 开发和回归只使用合成 origin、合成 AppID、合成家庭、临时数据库和系统临时目录；未连接、读取或复制生产数据，未写入真实手机号、AppSecret、证书、Profile、签名材料或真实设备标识。

## 本次完成

1. 新增 `npm run prepare:miniapp-synthetic`，要求同时提供获批的 canonical 非生产 HTTPS origin、格式正确且与 canonical 工程不同的 synthetic AppID、全新的系统临时输出目录，以及 origin/AppID 两项显式确认。
2. 输出只允许位于经 realpath 校验的本机系统临时目录；拒绝已有路径、仓库内或非临时路径、UNC、reparse 父目录、非 canonical basename 和私有/密钥/签名类输入。
3. 生成器只接受 stage-0 普通 Git 文件，并要求 index 与 `HEAD` 对整个小程序树的 mode、OID 和 path 完全一致；随后用 raw `git cat-file --batch` 读取 blob。未暂存工作树既不检查也不使用，被忽略的 `project.private.config.json` 不读取、不复制。
4. 只显式排除小程序 README；其他未知跟踪文件类型 fail closed。`project.config.json` 必须保持 miniprogram、空 pack include/ignore、空 Babel output，并拒绝 miniprogram/cloud/plugin 等外部 root。
5. 生成副本把 develop/trial 的 API 与法律源固定为获批 synthetic origin，并开启 synthetic 监护预览；release、unknown 和其他环境固定使用不可路由源且 `environmentReady=false`。不允许运行时 storage、ext config 或启动参数改源。
6. 副本使用操作员提供的 synthetic AppID、固定 synthetic project name、`urlCheck=true` 和 `uploadWithSourceMap=false`；不生成或复制私有配置、AppSecret、证书或签名材料。
7. 新增 legacy `/api/...` 路径规范器，userinfo、子域拼接、绝对 URL、双斜线、反斜杠、fragment、多问号和多层编码路径穿越均返回稳定 `CLIENT_REQUEST_INVALID`，且不调用 `wx.request`。
8. 小程序静态门锁定 app legacy、v2 transport、法律 URL 和唯一 legal web-view 的精确审计源码；全树只允许两处低层 request 出口。生成器进一步要求完整小程序输入树匹配本轮固定审计摘要，任一后续源码变化都必须重新人工审计并更新摘要。
9. 所有生成器 Git 命令固定 `--no-pager`、`--no-optional-locks`、`core.fsmonitor=false`、canonical `safe.directory` 和 `GIT_NO_LAZY_FETCH=1`，并清理仓库/配置注入环境；成功路径只允许 `rev-parse`、`ls-files --stage`、`ls-tree HEAD` 与 raw `cat-file --batch`，不执行会触发 attributes/filter 的 `status`。
10. staging 内独立验证精确文件集、每个文件字节、manifest、源/生成树摘要、名称摘要、patched 文件摘要和生成器实现摘要；目录原子 rename 是成功路径最后一次文件系统操作，不做发布后路径递归回滚。
11. manifest 明确区分离线证据与外部事实：只证明 synthetic AppID 字符串不同且操作员已确认；AppID provisioning、开发者权限、request/business domain、DevTools 私有设置和基础设施连通均记录为未验证。CLI 成功/失败不回显 origin、AppID 或本机路径。

## 验证结果

- `npm test`：212/212 通过，只使用临时 SQLite、合成对象与系统临时目录。
- 小程序环境、安全门和 synthetic 定向测试：28/28 通过；其中 synthetic 6/6 覆盖参数、路径、index/HEAD 来源、manifest 独立重算、develop/trial 双 transport、release/unknown 零请求、失败清理、rename-final 和 CLI 无外部 I/O。
- `npm run check`：通过，包含 rename、小程序和 HarmonyOS 静态安全门。
- `node --check scripts/prepare-miniapp-synthetic-workspace.js`、`node --check server/test/miniapp-synthetic-workspace.test.js`、`git diff --check`、各提交 `git diff --cached --check`：通过；只有 Git 行尾转换提示。
- 三路最终只读复核结论：P0 0、P1 0。已关闭 legacy origin 逃逸、发布后递归删除 TOCTOU、fsmonitor hook、promisor lazy fetch、attributes/process filter、manifest 自证和 AppID 过度声明等问题。

## 审计透明说明

- S11 开始时发现旧版 `scripts/check-miniapp.js` 会递归枚举真实小程序目录，从而可能读取被忽略的本机私有项目配置；`f38b443` 已改为只枚举经路径、模式和 realpath 校验的 Git 跟踪普通文件，并将运行环境候选按固定摘要在隔离 VM 中执行。
- 一次早期宽范围只读检索误命中该 ignored 私有配置的一行非秘密设置；未读取或复制完整文件，未使用其中任何值，后续生成器和验证均不依赖该文件。

## 明确未做

- 没有建立、连接或探测独立 synthetic API；测试 origin 只是边界样本，不表示 endpoint 存在。
- 没有验证 synthetic AppID 已独立注册、当前开发者有权限，或微信后台 request/business domain 已配置。
- 没有读取、生成或修改 `project.private.config.json`；也没有证明 DevTools 私有设置保持域名/TLS 校验。
- 没有在微信开发者工具编译 WXML/WXSS，没有模拟器或成人受控真机联网 smoke，没有 preview/upload/部署。
- 没有修改 canonical 小程序 develop/trial 零联网 profile，没有开放生产入口或儿童功能门。
- 没有创建签名包、RELEASE 证书/Profile、AppGallery 产物，也没有向未成年人设备分发或侧载。

## 下一步

1. 由项目明确批准并建立物理、域名、凭据和数据均独立的 synthetic API，完成 DNS/TLS、独立密钥、受控部署、合成家庭、边缘限流和最小监控；禁止复用生产 host、生产密钥或生产数据。
2. 外部确认独立 synthetic AppID provisioning、当前开发者权限、request 合法域名和法律 web-view business domain；人工检查 DevTools 私有配置未关闭域名/TLS 校验。
3. 只使用 S9 HarmonyOS 临时 profile 和 S11 小程序临时工程，在微信开发者工具、HarmonyOS 模拟器或成人受控设备完成 WXML/WXSS/ArkTS 编译与“授权 → 配对 → 查积分/规则 → 申报 → 审批 → 查看结果 → Refresh → 撤销”联网 smoke；证据不得含凭据、儿童信息或真实设备标识。
4. 验证 HUKS、AssetStore、前后台、重启、清数据、结果未知恢复、合法域名/TLS、法律页面重定向/CSP 和撤权即时失效；发布候选重跑 BUILD、Hypium、静态门和 CodeLinter。
5. 并行完成正式儿童易懂摘要、完整法律文本、处理者联系方式、客服/投诉、真实行权入口、安全文件导出/披露回执、未决申报文字保留期限和监护授权安全放弃策略。

## 仍未满足的生产硬门

- 正式法律文本与监护同意、PIPIA、存量儿童数据逐类整改、留存/删除/去标识化与不可变账本方案、受托方约束、儿童信息保护负责人和安全事件流程尚未完成。
- 生产密钥托管与轮换、数据库/对象存储/备份加密、迁移清单、恢复演练、监控、回滚和边缘限流尚未完成。
- 独立 synthetic 基础设施与 AppID/域名/DevTools 验证、成人受控预发布 E2E、HUKS/AssetStore 真机验证尚未完成。
- APP/小程序适用备案与审核、AppGallery 材料、内容分级、隐私标签、审核测试家庭、RELEASE 证书/Profile、候选包结构/签名/隐私验证和正式法律页面发布验证尚未完成。
- 上述硬门全部关闭前，所有儿童生产功能门继续默认关闭。S11 临时小程序工程不得用于 production preview/upload，也不得向未成年人设备分发。
- 不得开发侧载、切换孩子账号、关闭未成年人模式、开启孩子设备开发者模式或其他绕过正式分发的路线。
