# 糖罐积分阶段 1 / S4 积分申报交接（2026-08-23）

## 当前基线

- canonical 仓库：`C:\Users\ANGUS\projects\hefei-points`
- 当前分支：`codex/stage1-s4-point-requests-20260823`
- S4 代码提交：`201c094 feat(server): add point request approval ledger`
- 基于 S3 文档提交：`30df5db docs(stage1): record child self-only boundary`
- 仅创建本地提交；未 push、未部署、未修改远端 refs。
- 开发与回归只使用合成家庭和临时 SQLite；未连接、读取或复制生产数据，未写入真实手机号、AppSecret、证书、签名材料或设备标识。

## 已完成

1. 新增前向迁移 `008_point_requests_transaction_sources.sql`，建立积分申请、不可变成功事件、revision 状态机和交易来源约束；007→008 保留既有数据，旧流水来源保持为空。
2. 新增设备提交、本人列表、补充与取消 API，以及监护人待办列表/详情、退回补充、批准、驳回和家庭待办汇总。
3. 设备身份全部来自现有 Access 会话；最终业务查询或事务复验精确 session/binding、家庭/儿童、隐私状态、当前监护授权范围和既有积分账户。授权撤回继续即时阻断处理并联动撤销设备会话。
4. 同一绑定的 `clientRequestId` 与 canonical 请求指纹共同提供跨重启领域幂等；所有写 API 保存成功事件和幂等键 SHA-256 摘要。跨设备共享申请视图不回显来源绑定的客户端请求 ID。
5. 服务端保存 reward 规则快照。普通补报期限仍待产品确认，当前 fail-closed：`occurredAt` 必须是合法 RFC 3339、不得晚于提交时点，且只能落在上海当日；缺省使用服务端提交时点。
6. 批准、revision 推进、既有账户更新和唯一来源流水在同一 `BEGIN IMMEDIATE` 事务内完成。来源流水与申请快照强绑定且禁止改写、软删、硬删；未批准申请不入账，缺失账户不自动补建。
7. 设备与成人列表使用 `limit + 1` keyset 分页及 AEAD 不透明游标；游标绑定 audience、家庭、当前用户/设备绑定、儿童筛选和状态筛选。只读多查询路径使用 `BEGIN DEFERRED` 固定一致快照且不抢写保留锁。

架构边界见 [ADR-0007](../adr/0007-point-request-approval-ledger-boundary.md)，阶段状态见 [阶段 1 实施清单](../plans/阶段1-现有能力审计与首批实施清单-20260823.md)。

## 验证与复核

- `npm test`：109/109 通过。
- `node --test server/test/point-requests.test.js`：11/11 通过。
- `node --test server/test/point-requests-migration.test.js`：4/4 通过。
- `npm run check`：通过。
- `git diff --check`：通过。
- API、安全和测试三路只读复核均未发现剩余 P0/P1。

覆盖重点包括：100 次同请求并发重试、双监护人同 revision 竞争、审批精确重放/冲突/陈旧重放、事务故障回滚、账户丢失、授权撤回、家庭/兄弟姐妹/设备隔离、规则和发生时间快照、来源唯一与不可变、AEAD 游标防篡改和跨作用域拒绝，以及迁移重复前滚、完整性和外键检查。

## 已知非阻断项

- 旧 `/api/history/note`、`/delete` 和范围清理命中审批来源流水时，数据库会正确拒绝并保持完整性，但旧路由仍按既有契约返回 503。后续应决定混合清理是跳过受保护流水还是整体稳定拒绝，并映射为永久 409 和友好业务错误码。
- 普通申报补报期限的最终产品值仍待确认；确认前保持上海当日的收紧边界。

## 仍未满足的生产硬门

- 正式隐私政策、儿童个人信息保护规则、儿童用户协议及显著易懂告知尚未定稿发布。
- PIPIA、存量儿童数据盘点与整改、逐类留存/删除策略及数据行权闭环尚未完成。
- 生产密钥托管与轮换、迁移备份恢复演练、容量/告警和发布审查尚未完成。
- AppGallery 正式上架与未成年人模式安装验证尚未完成；不得开发侧载或绕过路线。
- 因此 Harmony、建档、设备、积分申报等儿童生产功能门必须继续默认关闭。

## 下一步

按阶段清单进入 S5/009 数据行权与审计：覆盖查阅/导出、更正、撤回授权、删除和终止服务的可审计状态机；继续只使用合成家庭和临时数据库，并维持授权撤回即时阻断与设备会话撤销边界。
