# 糖罐积分阶段 1 / S9 合成端到端就绪交接（2026-08-25）

## 当前基线

- canonical 仓库：`C:\Users\ANGUS\projects\hefei-points`
- 当前分支：`codex/stage1-s9-synthetic-e2e-20260825`
- 基于 S8 文档提交：`6f98766 docs(stage1): record HarmonyOS point requests slice`
- loopback E2E 提交：`4cd9f66 test(stage1): add synthetic loopback journey`
- synthetic profile 提交：`8e2a80c feat(harmonyos): prepare approved synthetic profile`
- 文档提交：本交接所在的 `docs(stage1): record synthetic e2e readiness` 提交
- 只创建本地分支和本地提交；未 push、未部署、未修改远端 refs，未执行 HAP/APP 打包、签名或 AppGallery 操作。
- 开发和回归只使用合成对象、合成家庭、临时 SQLite、loopback HTTP 与系统临时目录；未连接、读取或复制生产数据，未写入真实手机号、AppSecret、证书、Profile、签名材料或真实设备标识。
- 未读取、复制或依赖被忽略的本机 HarmonyOS 私有根构建配置；所有临时验证工作区均已删除。

## 本次完成

1. 新增阶段 1 loopback 全链 E2E，直接使用生产 `server/index.js#createApp`，固定 direct 客户端地址语义，并只监听 `127.0.0.1:0` 随机端口。
2. 测试在独立临时 `DATA_DIR` 和 SQLite 中创建两个合成家庭、合成成人、四类合成法律证据与合成规则，不读取仓库运行数据；退出时关闭监听器和数据库并删除临时目录。
3. 全链覆盖成人登录、公开法律证据、用途绑定重新认证、授权建档、一次性短码、设备 claim、家长二次确认、真实 P-256 proof 和设备 Access/Refresh 签发。
4. 设备以 Access 读取本人摘要和当前 reward rules，提交文字积分申请；家长待办审批后只生成一条来源流水并原子更新余额。
5. 测试模拟创建响应被客户端丢弃，重启 loopback 监听器后用原 body 和原幂等键恢复；同一 `clientRequestId` 在数据库仍只有一条申请。
6. 第二家庭列表为空，对第一家庭申请的详情和审批都稳定返回 `POINT_REQUEST_NOT_FOUND`，避免用 403 暴露对象存在性。
7. 设备在真实刷新窗口内获取 challenge、用 P-256 proof 轮换 Access/Refresh，并验证原 Refresh 加原键重放恢复相同秘密、旧 Access 立即 `SESSION_REVOKED`。
8. 家长撤回监护授权后，新 Access 和新 Refresh 均立即 `SESSION_REVOKED`，目标家庭不再存在 active device session。
9. HarmonyOS `ApiEnvironment` 增加 canonical 公共 HTTPS origin 校验；拒绝 HTTP、大小写非规范 host、userinfo、端口、路径、查询、IP 和保留测试后缀。跟踪 profile 仍严格为 `develop-blocked`、`NETWORK_ENABLED=false`、`https://harmony-child.invalid`。
10. 新增 `npm run prepare:harmonyos-synthetic`。只有显式确认获批非生产 origin 后，才在本机系统临时目录创建全新、unsigned 的 `synthetic-approved` 工作区；生成命令本身不连接该 origin。
11. 生成器拒绝生产域、UNC/设备路径、仓库内或非临时目录、已存在输出、reparse 父目录、非普通 Git mode、工作树符号链接、子模块和非允许扩展。根私有 `build-profile.json5` 若被跟踪会立即失败；输出 build profile 的签名配置为空。
12. 生成器复制前后核对同一 HEAD、同一跟踪文件列表、同一 Git 状态和同一 working-tree SHA-256；manifest 明确记录 commit、源树摘要、dirty、tracked-only、unsigned、temporary 和外部基础设施仍需审批。
13. CLI 成功或失败均不回显 origin 或本机路径；回归通过 preload 阻断 `net`、`tls`、`http`、`https`、`http2`、`dgram`、DNS 和 `fetch`，防止以后无意把“准备工作区”变成联网动作。
14. HarmonyOS 静态门现在要求 `ApiEnvironment.ets` 存在，精确固定跟踪 profile/origin，并把 canonical validator 的语义骨架和调用位置限定在 `assertUsable`；对删除文件、拼接 origin、移出校验和符号链接逃逸都有对抗回归。

## 验证与安全复核

- `npm test`：199/199 通过。
- S9 定向回归：16/16 通过，包含 loopback journey、生成器和 HarmonyOS 静态对抗测试。
- `npm run check`：通过，包含 rename、小程序和 HarmonyOS 静态安全门。
- HarmonyOS 静态安全门：10/10 通过。
- 从干净 `8e2a80c` 生成的临时 unsigned profile：manifest 显示 `sourceTrackedChanges=false`、来源 commit 匹配、源树 SHA-256 合法；Hvigor BUILD SUCCESSFUL，`test_result.txt` 明确为 39/39。
- `node --check scripts/prepare-harmonyos-synthetic-workspace.js`、`git diff --check` 与各提交 `git diff --cached --check`：通过；只有工作区行尾转换提示。
- 最终只读安全复核确认无剩余 P0/P1，无生产数据、真实凭据、私有构建配置读取、生产源泄漏或外网调用。
- ArkTS CodeLinter 本轮未计为有效新证据：本机 CLI 报告无法解析已安装 HarmonyOS SDK，尽管同一临时工程的 Hvigor ArkTS 编译和 Hypium 全部通过。S8 的 0 缺陷是历史证据；发布候选前必须修复该工具链并重跑。

## 本切片没有完成什么

- 没有建立或连接独立 synthetic API；`synthetic-api.example.com` 只用于本地验证 origin 边界，不对应已部署服务。
- 没有配置 DNS、TLS 证书、独立服务端密钥、基础设施账号、边缘限流、容量、监控或告警。
- 没有在 HarmonyOS 模拟器或成人受控设备执行联网 smoke，也没有验证真实 HUKS、AssetStore、锁屏、前后台、进程重启、清数据或撤权恢复。
- 没有为微信小程序 `develop`/`trial` 开启网络；其跟踪配置继续 fail closed。
- 没有实现儿童易懂版隐私摘要、设置、申请补充/取消/重新提交、审核测试家庭、安全文件交付或完整成人预验收。
- 没有打包、签名、部署、上架或向未成年人设备分发任何产物。

## 下一步

1. 由项目明确批准并建立物理、域名、凭据和数据均独立的 synthetic API，完成 DNS/TLS、独立密钥、受控部署、合成家庭、边缘限流和最小监控；禁止复用生产 host、生产密钥或生产数据。
2. 只用 S9 生成器产生的系统临时 HarmonyOS profile，以及后续同等级的小程序编译时配置连接该环境；跟踪默认值继续保持零联网，不允许运行时覆盖 origin。
3. 在 HarmonyOS 模拟器或成人受控 API 20+ 设备及微信开发者工具完成“授权 → 配对 → 查积分/规则 → 申报 → 家长审批 → 查看结果 → Refresh → 撤销”联网 smoke；证据只保存稳定测试名、状态和计数，不保存短码、Token、幂等键、签名、公钥、设备 ID 或申报正文。
4. 验证 HUKS、AssetStore、前后台、进程重启、锁屏、清数据和撤权恢复；同时修复 CodeLinter SDK 解析并重跑最终 ArkTS 主源码。
5. 并行实现不冒充正式法律文本的儿童设置/隐私安全壳，并由合规工作流定稿儿童易懂摘要、联系方式、数据清单和行权说明。

## 仍未满足的生产硬门

- 正式《隐私政策》《儿童个人信息保护规则》《儿童用户协议》、监护人同意、关系声明、儿童易懂摘要、客服/投诉入口和公开法律页面尚未定稿发布。
- PIPIA、存量儿童数据逐项整改、逐类留存/删除表、备份同步清理、不可变账本去标识/密钥销毁方案尚未完成。
- 受托处理方约束、生产密钥托管与轮换、数据库/对象存储/备份加密、迁移与恢复演练、删除作业端到端验证尚未完成。
- APP/小程序适用备案与审核、AppGallery 材料、内容分级、隐私标签、审核测试家庭、RELEASE 证书/Profile、候选包结构/签名/隐私验证，以及成人预发布 smoke 都是硬门。
- 不得开发侧载、切换孩子账号、关闭未成年人模式、开启孩子设备开发者模式或其他绕过正式分发的路线。
- 因此 Harmony、建档、设备、积分申报、普通数据行权新建和 legacy 儿童入口等生产功能门继续默认关闭；撤回、删除、终止和既有回执可达不等于允许开放儿童生产功能。
