# 糖罐积分阶段 1 / S10 儿童设置与隐私安全壳交接（2026-08-26）

## 当前基线

- canonical 仓库：`C:\Users\ANGUS\projects\hefei-points`
- 当前分支：`codex/stage1-s10-child-privacy-settings-20260825`
- 基于 S9 文档提交：`6d53b9f docs(stage1): record synthetic e2e readiness`
- 功能提交：`08de124 feat(harmonyos): add local privacy safety shell`
- 文档提交：本交接所在的 `docs(stage1): record local privacy safety shell` 提交
- 只创建本地分支和本地提交；未 push、未部署、未修改远端 refs，未执行 HAP/APP 打包、签名或 AppGallery 操作。
- 开发和回归只使用固定工程数据、合成对象、临时 SQLite 与系统临时 unsigned 工作区；未连接、读取或复制生产数据，未写入真实手机号、AppSecret、证书、Profile、签名材料或真实设备标识。
- 本切片未输出、复制、使用或依赖被忽略的本机 HarmonyOS 私有根构建配置中的任何值；编译和静态验证只使用 Git 跟踪源码及临时 unsigned profile。

## 本次完成

1. 在现有 `Index` 单页增加“设置与隐私”入口和纯本地“设置与数据安全”条件视图，不新增路由，也不改变跟踪配置的零联网默认值。
2. 安全壳只从固定本地模型展示通用设备别名、版本、受控网络门、后台清理、通知/照片/备份状态、数据类别、最小可见范围、家长侧操作提示和生产硬门。
3. 页面显著标记 `S10_LOCAL_SAFETY_SHELL_NOT_FORMAL_LEGAL_TEXT`，明确不是正式《隐私政策》《儿童个人信息保护规则》《儿童用户协议》或同意页面。
4. 安全壳不读取动态儿童、家庭、设备、绑定、会话、Token 或凭据，不联网、不持久化、不导航，也不提供解绑、撤权、删除或“已同意”等假动作。
5. 切换业务页与安全壳前会清除配对短码和积分申报草稿；进入后台时复位安全壳，并继续执行既有锁定、短码、草稿和业务视图清理。
6. 静态门把 `main_pages.json` 封闭为唯一可达的 `Index` 页面，并要求 `Index.ets` 内只有一个 `@Entry @Component struct Index`；同时精确限制 imports、安全壳导出面、受信 Builder/helper/生命周期签名和关键调用顺序。
7. 对安全壳 header、build、render、toggle、网络状态和清草稿方法增加标识符、事件、动态数据和副作用 allowlist，阻止额外网络、存储、路由或业务数据渗入。
8. 新增 decoy class/struct/Entry、re-export、允许名遮蔽、非白名单模板插值、成员调用、默认参数、可选/计算属性、helper 副作用、额外后台动作和注释/正则歧义等对抗夹具。
9. 新增 ArkTS 单测验证固定安全模型、正式文本边界、无 URL/API/动态标识、两种网络文案和生产硬门；根静态安全测试另行验证调用顺序、无操作项、源码形态和恶意夹具。
10. 使用本机 DevEco 随附的 CodeLinter CLI 包装入口识别已安装 SDK 并完成有效检查；仅保留一个既有启动图标尺寸 suggestion，本轮未用通用模板或生成式图标替换品牌资源。

## 验证与安全复核

- `npm test`：200/200 通过。
- HarmonyOS 静态安全门：11/11 通过，包含 S10 结构化约束和对抗夹具。
- `npm run check`：通过。
- `git diff --check` 与功能提交前的 `git diff --cached --check`：通过；只有工作区行尾转换提示。
- Git 跟踪源码复制到临时 unsigned 工作区后 Hvigor BUILD SUCCESSFUL，Hypium `test_result.txt` 为 43/43。
- ArkTS CodeLinter：0 error、0 warning、1 suggestion。唯一建议是现有 1024×1024 启动图标宜从获批品牌源导出为不超过 256×256；候选替代图未进入仓库，现有资源未修改。
- 两路最终安全/门禁复核均确认无剩余 P0/P1。已知 P2 是正则静态门属于 defense-in-depth，不能替代 ArkTS 编译器和涉及儿童信息流时的人工审查；其格式严格性和跨 helper 语义也须继续人工复核。
- 未连接外部网络或业务服务，未打包、签名、部署、上架或向未成年人设备分发任何产物。

## 本切片没有完成什么

- 没有建立或连接独立 synthetic API，也没有为微信小程序开启 develop/trial 网络。
- 没有在 HarmonyOS 模拟器或成人受控设备执行联网 smoke，没有验证真实 HUKS、AssetStore、锁屏、前后台、进程重启、清数据或撤权恢复。
- 没有定稿正式儿童易懂摘要、三份法律文本、处理者联系方式、客服/投诉、同意或真实行权入口，也没有完成 PIPIA。
- 没有增加解绑、删除、撤权或同意动作；S10 工程说明不能被产品或审核材料冒充为这些能力。
- 没有实现申请补充、取消或重新提交；设备作用域单条详情和写操作对账契约仍是前置条件。
- 没有创建 RELEASE 证书/Profile、候选 HAP/APP、签名包、部署、AppGallery 提交或儿童分发路线。

## 下一步

1. 经项目明确批准后，建立物理、域名、凭据和数据均独立的 synthetic API，完成 DNS/TLS、独立密钥、受控部署、合成家庭、边缘限流和最小监控；禁止复用生产 host、生产密钥或生产数据。
2. 只用系统临时 HarmonyOS profile 和同等级的小程序编译时配置连接该环境；跟踪默认值继续保持零联网，不允许运行时覆盖 origin。
3. 在 HarmonyOS 模拟器或成人受控 API 20+ 设备及微信开发者工具完成“授权 → 配对 → 查积分/规则 → 申报 → 家长审批 → 查看结果 → Refresh → 撤销”联网 smoke；证据不得保存短码、Token、签名、公钥、设备 ID、幂等键或申报正文。
4. 验证 HUKS、AssetStore、前后台、进程重启、锁屏、清数据、结果未知恢复和撤权联动，并在最终 ArkTS 主源码上重跑编译、Hypium、静态门及 CodeLinter。
5. 由合规工作流定稿正式儿童易懂摘要、完整法律文本、处理者联系方式、客服/投诉和真实行权入口，并由产品与可访问性评审 S10 后续正式页面。
6. 从获批品牌源导出不超过 256×256 的准确启动图标，再以最终资源重跑 CodeLinter；不得用通用或生成式候选替代品牌资产。

## 仍未满足的生产硬门

- 正式《隐私政策》《儿童个人信息保护规则》《儿童用户协议》、监护人同意、关系声明、儿童易懂摘要、处理者联系方式、客服/投诉入口和公开法律页面尚未定稿发布。
- PIPIA、存量儿童数据逐项整改、逐类留存/删除表、备份同步清理、不可变账本去标识/密钥销毁方案尚未完成。
- 受托处理方约束、生产密钥托管与轮换、数据库/对象存储/备份加密、迁移与恢复演练、删除作业端到端验证、监控和安全事件流程尚未完成。
- 独立 synthetic 环境、成人受控预发布 smoke、HUKS/AssetStore 运行验证、APP/小程序适用备案与审核、AppGallery 材料、内容分级、隐私标签、审核测试家庭、RELEASE 证书/Profile、候选包结构/签名/隐私验证仍是硬门。
- 不得开发侧载、切换孩子账号、关闭未成年人模式、开启孩子设备开发者模式或其他绕过正式分发的路线。
- 因此 Harmony、建档、设备、积分申报、普通数据行权新建和 legacy 儿童入口等生产功能门继续默认关闭；撤回、删除、终止和既有回执可达不等于允许开放儿童生产功能。
