# 阶段 1：现有能力审计与首批实施清单

- 日期：2026-08-23（2026-08-24 更新 S6 安全加固）
- 状态：S0、S1/006、S2/007、S3、S4/008、S5/009、S6 及其安全加固已在本地完成；未部署、未开放生产
- 基线：`codex/repo-baseline-20260820@921e32a`
- 审计分支：`codex/stage1-reuse-audit-20260823`
- S1 实现分支：`codex/stage1-s1-enrollment-20260823`
- S2 实现分支：`codex/stage1-s2-device-pairing-20260823`
- S3 实现分支：`codex/stage1-s3-child-self-views-20260823`
- S4 实现分支：`codex/stage1-s4-point-requests-20260823`
- S5 实现分支：`codex/stage1-s5-data-rights-20260823`
- S6 实现分支：`codex/stage1-s6-miniapp-guardian-20260824`
- S6 安全加固分支：`codex/stage1-s6-miniapp-hardening-20260824`
- 总体依据：[糖罐积分 HarmonyOS 版 MVP 方案与推进计划](糖罐积分鸿蒙版-MVP方案与推进计划.md)

## 1. 结论

现有 Express、SQLite 和微信小程序可以作为阶段 1 的工程底座，不需要重写。家庭隔离、积分账户与流水、稳定规则 ID、规则版本、事务封装、`revision` 条件写、临时数据库测试和小程序统一请求封装都可以复用。

但是，现有儿童账号、旧 Token、积分与历史查询、儿童创建和数据清理接口不能直接供 HarmonyOS 儿童端使用。它们目前不满足监护人授权、孩子本人最小可见、设备绑定、会话轮换、审批幂等和可审计数据行权等要求。

阶段 1 必须先完成安全前置，再依次实现“授权建档 → 设备配对 → 本人查询 → 积分申报审批 → 数据行权”。全部儿童生产功能默认关闭，只允许用合成家庭验证；本文件不代表已经对真实儿童数据完成盘点、取得授权或开放生产。

## 2. 审计边界

本轮只读取源码、工程文档和合成测试，不读取 `data/`、`data-dev/`、`backups/`、日志、真实账号、签名材料或发布凭据，不修改远端和父仓。

验证结果：

- 审计基线 `npm test`：29/29 通过；
- S2/007 完成后 `npm test`：86/86 通过；
- S3 完成后 `npm test`：94/94 通过；
- S5/009 完成后 `npm test`：119/119 通过；
- S6 完成后 `npm test`：156/156 通过；
- S6 安全加固后 `npm test`：182/182 通过；
- `npm run check`：通过；
- 审计开始时工作树干净。

存量儿童数据的合法性、保留依据、备份同步删除和 PIPIA 仍是独立合规工作流，不能用本次代码审计替代。

## 3. 可直接复用的能力

### 3.1 后端

| 能力 | 现状 | 阶段 1 用法 |
| --- | --- | --- |
| Express 模块化路由 | 入口可挂载独立路由 | 新增 `/api/v2`，不改变旧家长端接口语义 |
| SQLite 增量迁移 | 有迁移登记、WAL、`BEGIN IMMEDIATE` 和统一事务 | 继续使用独立编号迁移；补迁移前备份和前滚/代码回退测试 |
| 家庭、用户、积分账户和流水 | 已有基础表和家庭作用域 | 作为账本底座；儿童设备端只能走本人专用接口 |
| 积分原子入账 | 余额更新和流水插入在同一事务 | 抽出可接收现有事务的底层函数，供审批事务调用 |
| 稳定规则/分类 ID | 服务端已有 ID、分值校验和名称快照 | 申报保存规则快照，不能仅保存可变名称 |
| `revision` 条件写 | 规则并发修改已有“一成一败”实现 | 复用到申请审批、授权和设备确认 |
| 测试框架 | 临时 SQLite、合成家庭、临时 HTTP 服务和原生 `fetch` | 新测试继续禁止连接真实数据 |
| 请求 ID、结构化日志和外围限流 | 已有通用中间件 | 继续使用；儿童审计日志改为字段白名单，短码锁定必须落库 |

### 3.2 微信小程序

| 能力 | 现状 | 阶段 1/2 用法 |
| --- | --- | --- |
| `fetchAPI` 统一请求 | 已集中封装 Token 与错误处理 | 扩展 v2 错误码、幂等键、`expectedRevision`、短期重新认证凭据 |
| 家庭会话与孩子卡片 | 已有登录态、家庭作用域和孩子选择器 | 复用为授权后选择孩子、创建设备配对任务 |
| 记录列表视觉结构 | 已有筛选、分组和详情弹窗 | 复用列表样式；申请审批必须使用独立数据模型和详情组件 |
| 分值调整弹窗 | 已有数字与备注输入 | 复用为家长审批时调整批准分值 |
| “我的”页面 | 已有常驻入口 | 增加“家庭与隐私”：授权、设备、数据权利和处理回执 |
| 家庭作用域本地存储 | 已有分家庭 key 设计 | 只保存授权前本地孩子别名草稿，不得提前同步儿童业务数据 |

## 4. 必须先处理的 P0 问题

### P0-1：未登录可枚举全部家庭成员

`GET /api/config` 在没有 Token 时调用 `users.listAll()`，可能返回所有家庭成员的 ID、姓名、角色和家庭 ID。阶段 1 的第一项代码改动必须移除匿名成员列表；小程序未登录首页不得再依赖该响应展示用户。

验收：匿名调用不返回任何用户、孩子或家庭关联；旧家长登录方式仍可用；有安全回归测试。

### P0-2：现有儿童创建绕过监护人授权

`POST /api/family/child/create` 可直接创建孩子，且缺省密码为 `123456`；`/api/config/users` 也可以批量改写儿童。家庭管理员身份不等于已经验证的法定监护关系。

验收：旧儿童新增能力受默认关闭的服务端开关约束；新孩子只能通过原子的 `POST /api/v2/child-enrollments` 创建。事务必须同时验证短期重新认证、关系声明、三份公开文本和敏感信息单独告知同意，任一失败均不得产生孩子或授权记录。禁止固定默认密码。

### P0-3：儿童旧 Token 可读取兄弟姐妹数据

`GET /api/points` 会返回全家庭积分账户，`GET /api/history` 在未提供或提供无效 `kid` 时可能退化为全家庭流水。

验收：旧接口对 `child` 角色收窄并有回归测试；HarmonyOS 只使用 `/api/v2/me/summary`、`/api/v2/me/transactions` 和 `/api/v2/me/point-requests`。`childId` 必须从设备会话推导，客户端不能指定其他孩子。

实施状态：已完成 S3 本人只读切片。两个 `/api/v2/me` 接口严格只接受设备 Access Bearer；家庭、儿童、绑定和会话均来自认证上下文，业务查询再次复验 active session、binding、同一监护人的当前 active 授权、隐私状态、儿童角色以及 `pointsLedger/self_only` 范围。绑定仍保留创建时授权版本作为签发审计证据；正常续签后既有设备继续可用，当前授权撤回后立即失效。摘要不接受查询参数；流水只接受有上限的 `limit` 和作用域绑定的 AEAD 不透明游标，软删除、兄弟姐妹、跨家庭及内部字段不会进入响应。

### P0-4：当前备份发生在迁移之后

数据库连接会自动执行迁移，而现有备份脚本通过同一连接打开数据库，所以不能满足“旧库备份 → 校验 → 迁移”。旧 JSON 迁移脚本还在使用已经过时的规则表写法。

验收：提供不加载应用连接模块的迁移前备份流程，先复制数据库及 WAL、生成 SHA-256、执行 `PRAGMA integrity_check`，再启动兼容代码执行迁移；旧 JSON 迁移脚本修复并以临时库测试，或明确归档为不可用。

实施状态：已完成。`scripts/pre-migration-backup.js` 不加载应用连接，使用 SQLite `VACUUM INTO` 生成包含已提交 WAL 内容的一致性快照，并生成文件 SHA-256、全库逻辑内容 SHA-256、完整性、外键、迁移集合与业务计数清单。生产连接在有存量数据和待迁移版本时必须校验 `PRE_MIGRATION_BACKUP_MANIFEST`，并重算当前源库逻辑摘要；缺失、陈旧或不匹配时拒绝迁移。旧 JSON 迁移脚本的单独归档/重写仍留作后续历史清理，不用于 006 发布。

### P0-5：旧会话不能作为儿童设备会话

当前 Token 最长 30 天，自定义 HMAC 签名只截取 16 个十六进制字符，支持从 query/body 读取，没有设备绑定、短期 Access Token、Refresh 轮换、重用检测或持久会话。`/wx-bind` 还信任客户端提交的 `openid`。

验收：v2 只接受 `Authorization`；设备 Token 固定绑定 `familyId + childId + deviceBindingId`；Access 短期、Refresh 摘要落库并单次轮换；旧 Refresh 重用撤销整组；撤销设备或授权后立即失效；`openid` 只能由服务端换码得到。

实施状态：已完成。设备 claim、Access 和 Refresh 严格只接受 Bearer；Access 为 15 分钟，Refresh 为 30 天且只在 Access 剩余 5 分钟内签发单个 pending proof challenge。Refresh 按 token family 单次轮换，旧凭据非精确重试或超过固定幂等宽限后会撤销整组。设备、会话、授权撤回和儿童处理阻断均在持久状态上即时生效。

## 5. 不得误复用的旧能力

- 微信登录页的“同意授权”只是平台登录提示，不是监护人同意证据，必须改成准确文案。
- 家庭 6 位邀请码有效期和重用模型不同，不能复用为设备配对码。
- 家长直接调用 `/api/points/change` 的随手记能力不能作为儿童申报审批。
- `/api/history/delete` 仅隐藏流水且不冲正余额，不能删除审批流水；更正必须新增反向流水。
- 管理员“删除用户”和“清理历史”不能作为儿童查阅、删除或终止服务流程。
- 家庭 `admin/parent` 角色不能直接等同于已声明并验证的监护人。
- 现有内存限流重启后丢失，只能作为外围保护；短码失败计数和锁定必须持久化。

## 6. 迁移顺序

实际编号以当前 `005` 后续为准，每批独立迁移、独立测试，生产回滚采用“关闭功能开关 + 回退代码 + 保留新表数据”，不得直接删除授权、申请或审计数据。

### 006：授权与儿童建档

建议文件：`006_guardian_consent_enrollment.sql`

- `legal_text_versions`：四类服务端认可文本的版本、SHA-256、公开 URL 和生效时间；
- `reauth_assertions`：绑定用户、家庭和用途的 5 分钟一次性断言，只保存随机令牌摘要；
- `child_privacy_states`：`suspended_pending_consent`、`active`、`processing_blocked`、`deletion_pending`、`deidentified`、`deleted`；未授权孩子别名只允许保存在家长端本地草稿中，服务端不建草稿；
- `guardian_consents`：不可变授权版本、监护关系声明版本、四类文本版本与摘要、授权范围、撤回与替代关系；
- 新建孩子的密码登录固定关闭，孩子身份只能来自后续设备绑定；
- 存量儿童默认进入 `suspended_pending_consent`，在完成独立合规盘点前不得自动激活。

### 007：设备配对与会话

建议文件：`007_device_pairing_sessions.sql`

- `pairing_challenges`：短期、单次、失败次数、锁定和状态；
- `device_bindings`：随机公开 ID、公钥、孩子和有效授权版本；
- `device_sessions`：Access/Refresh 生命周期、轮换组和撤销；
- `device_session_challenges`：签名 challenge 单次消费，防重放。

数据库只保存设备配对短码的带密钥 HMAC 索引，不保存明文；配对申领只返回不透明 claim ID。

实施状态：已完成。短码和持久失败主体使用分域 HMAC，claim、Access、Refresh 和 proof challenge 只落摘要；家长须使用独立高熵 challenge 和 `expectedRevision` 二次确认。设备使用 canonical P-256 SPKI 对分域 signing payload 签名，只有 proof 成功后设备身份才取得 active 唯一性，避免公开身份在 proof 前被抢注。授权撤回、设备撤销、会话撤销、配对/证明过期或锁定均通过事务和数据库触发器撤销相关安全产物。

### 008：积分申报与来源唯一性

建议文件：`008_point_requests_transaction_sources.sql`

- `point_requests`：状态、`revision`、规则快照、申报分值、批准分值、说明、客户端请求 ID；
- `transactions.source_type/source_id`；
- `UNIQUE(device_binding_id, client_request_id)`；
- 同一家庭同一业务来源只允许一条有效流水的唯一约束；
- 家庭待办、孩子本人状态和疑似周期重复索引。

实施状态：已在本地完成，尚未部署。设备只可提交当前家庭 reward 规则，服务端保存规则版本、名称和范围快照；同一绑定 `clientRequestId` 与请求指纹共同实现跨重启领域幂等，疑似同周期重复只标记不丢弃。申请转换有 revision、不可变事件和幂等摘要证据；成人每次列表或审批均复验本人当前监护授权范围。批准、既有账户更新和唯一来源流水共用 `BEGIN IMMEDIATE`，带来源流水由数据库禁止改写、软删或硬删。授权撤回后待审证据保留，但设备会话撤销且隐私阻断使任何后续处理无法发生。迁移与 API 回归仅使用临时 SQLite 和合成家庭。

### 009：数据行权与审计

建议文件：`009_data_rights_audit.sql`

- `data_rights_requests`；
- 字段白名单 `audit_events`；
- 如删除异步执行，增加 `data_deletion_jobs`；
- 撤回授权事务同步暂停儿童处理、撤销设备绑定和所有设备会话。

实施状态：已在本地完成，尚未部署。唯一创建入口按儿童资源提供 `access/export/correct/delete/terminate`；既有授权撤回入口在同一事务补写 009 请求和不可变事件。请求绑定当前家庭成人本人最新的 active/withdrawn/superseded 监护证据、动作专用已消费 reauth、revision 和持久幂等摘要；列表使用绑定家庭、主体与筛选条件的 AEAD keyset 游标。导出只凭短时有效的已完成 access/export 请求动态生成，不持久化导出正文，并排除密码、密钥、公钥、凭据/幂等摘要和 challenge。首版更正只更新当前儿童别名，历史积分流水、申请、事件和规则/别名快照均保持不可变。删除或终止只原子进入 `deletion_pending`、撤销目标儿童全部设备安全产物和旧 Token、生成永久锁定为 `blocked_policy/policy_pending` 的作业；正式留存决定获批前数据库禁止进入 completed/deidentified/deleted。迁移与 API 回归只使用临时 SQLite 和合成家庭。

首版不加入照片、Push、用机兑换、第三方统计、`time_redemptions` 或通知 Outbox。

## 7. v2 API 完整清单

### 公共法律文本与重新认证

- `GET /api/v2/legal-texts/current`
- `POST /api/v2/reauth-assertions`

### 授权和儿童建档

- `POST /api/v2/child-enrollments`
- `GET /api/v2/children/:id/consents`
- `POST /api/v2/children/:id/consents`
- `POST /api/v2/children/:id/consents/withdraw`

### 设备

- `POST /api/v2/device-pairings`
- `GET /api/v2/device-pairings/:id`
- `POST /api/v2/device-pairings/claim-by-code`
- `POST /api/v2/device-pairings/:id/confirm`
- `POST /api/v2/device-pairings/claim/complete`
- `GET /api/v2/devices`
- `DELETE /api/v2/devices/:id`
- `POST /api/v2/devices/:id/session-challenges`
- `POST /api/v2/device-sessions/refresh`
- `DELETE /api/v2/device-sessions/:id`

### 孩子本人和积分申报

- `GET /api/v2/me/summary`
- `GET /api/v2/me/transactions`
- `GET /api/v2/me/point-requests`
- `POST /api/v2/point-requests`
- `GET /api/v2/point-requests/:id`
- `PATCH /api/v2/point-requests/:id`
- `POST /api/v2/point-requests/:id/request-info`
- `POST /api/v2/point-requests/:id/approve`
- `POST /api/v2/point-requests/:id/reject`
- `POST /api/v2/point-requests/:id/cancel`

### 数据权利

- `GET /api/v2/data-rights-operations/request-create`
- `GET /api/v2/data-rights-requests`
- `GET /api/v2/data-rights-requests/:id`
- `GET /api/v2/children/:id/data-export`
- `POST /api/v2/children/:id/data-rights-requests`

所有状态变更请求必须携带幂等键；涉及并发决定时携带 `expectedRevision`。统一返回稳定错误码，至少包括 `FEATURE_DISABLED`、`CONSENT_REQUIRED`、`REAUTH_REQUIRED`、`FORBIDDEN_SCOPE`、`VALIDATION_ERROR`、`CHILD_DATA_INCOMPLETE`、`REVISION_CONFLICT`、`IDEMPOTENCY_CONFLICT`、`PAIRING_EXPIRED`、`PAIRING_LOCKED` 和 `SESSION_REVOKED`。

## 8. 纵向实施切片与本地提交边界

每个切片独立提交、独立测试，不把整个阶段 1 塞入一个大提交。

### S0：安全前置

提交建议：`fix(server): close legacy child data exposure`

- 匿名 `/api/config` 不再返回成员；
- 旧儿童新增入口置于默认关闭开关；
- 旧 `child` 积分/历史查询限制为本人；
- `/api/v2` 鉴权上下文、稳定错误结构和全局开关骨架；
- 微信换码绑定不再信任客户端 `openid`；
- 新增 `v2-security-regression.test.js`。

退出条件：未登录、跨家庭和同家庭跨孩子越权测试全部通过，旧家长端回归通过。

### S1：授权建档

提交建议：`feat(server): add guardian consent enrollment`

- 006 迁移、法律文本、一次性重新认证、仓储、服务和 API；
- 原子建档；
- 存量儿童暂停状态；
- 不可变授权版本；
- `guardian-consents.test.js` 和迁移测试。

退出条件：拒绝任一文本或缺少重新认证均不建档；家庭管理员不会自动成为监护人；撤回立即阻止新增处理。

实施状态：已在本地完成，尚未部署。006 不预置伪造法律文本；四类文本和监护关系声明必须提供版本、SHA-256 与公开地址。建档、零余额账户、授权、隐私状态和幂等记录同事务完成。存量儿童回填为暂停态；旧成人接口只允许本人有效授权且状态 active 的儿童。任一监护人撤回会全局暂停，其他监护人不能覆盖；状态漂移后的旧幂等键返回 `IDEMPOTENCY_REPLAY_STALE`。

### S2：设备配对与会话

提交建议：`feat(server): add child device pairing sessions`

- 007 迁移和 API；
- 短码 HMAC、持久失败锁定、家长二次确认、公钥 challenge；
- Access/Refresh 轮换与撤销；
- `device-pairing-sessions.test.js`。

退出条件：过期、重放、猜码、跨家庭确认、旧 Refresh 重用和撤销后继续访问全部失败。

实施状态：已在本地完成，尚未部署。007 前滚保留既有业务与授权数据；家庭/儿童/设备复合外键、状态触发器、active 身份唯一性、会话代际和 challenge 结果链路均由数据库复核。API 覆盖创建、申领、家长确认、proof 完成、设备/会话列举与撤销、Refresh challenge 和轮换；稳定错误覆盖过期、锁定、作用域、revision、幂等冲突、提前刷新、凭据重用和撤销。所有测试仅使用合成家庭与临时 SQLite。

### S3：孩子本人只读

提交建议：`feat(server): add child self-only views`

- `/me/summary`、`/me/transactions`；
- 只返回当前绑定孩子；
- 关闭功能门时不处理儿童业务数据。

退出条件：孩子 A 无法通过参数、ID 或错误回退读取孩子 B。

实施状态：已在本地完成，尚未部署。摘要只返回本人 ID、别名和余额；流水按插入顺序使用 `limit + 1` 的 rowid keyset 分页，服务端将边界与交易 ID 摘要封装进绑定家庭、儿童和设备作用域的 AEAD 不透明游标，响应只返回业务 transaction ID、发生时间、分值、原因快照及可空规则/分类 ID。客户端身份选择、GET body、未知查询参数、畸形或跨绑定游标均稳定拒绝；设备、会话或授权撤销后旧 Access 立即失效，关闭 Harmony/Device 任一功能门时在业务查询前止损。所有回归只使用合成家庭和临时 SQLite。

### S4：积分申报审批

提交建议：`feat(server): add point request approval ledger`

- 008 迁移、状态机和家庭待办 API；
- `pending → needs_info → pending/approved/rejected/cancelled`；
- 审批和积分入账在同一数据库事务；
- `point-requests.test.js`。

退出条件：同一 `clientRequestId` 并发 100 次只产生一条申请；双亲同 revision 审批仅一次成功；未批准不入账；同一申请只产生一条来源流水。

实施状态：已在本地完成，尚未部署。已提供孩子提交、本人列表、补充、取消，监护人待办/详情、退回、批准、驳回及家庭待办汇总；设备和成人列表用 `limit + 1` keyset 与绑定家庭、主体、儿童筛选和状态的 AEAD 不透明游标分页，跨设备共享视图仅向来源绑定回显客户端请求 ID。规则变更后仍按提交快照审批；普通补报期限待确认，当前 fail-closed 为上海当日。100 次相同提交只产生一条申请，双监护人同 revision 只允许一次批准，故障注入会完整回滚申请、事件、余额和流水，缺失积分账户不会被自动补建。数据库迁移测试覆盖 007→008 数据保留、家庭/设备复合约束、状态机、来源完整性、旧流水兼容、重复迁移及 `integrity_check`/`foreign_key_check`。

### S5：数据行权

提交建议：`feat(server): add child data rights workflow`

- 009 迁移和可审计状态机；
- 查阅/导出、更正、撤回、删除和终止服务；
- 保留决定、删除作业和回执；
- `data-rights.test.js`。

退出条件：撤回立即停止新增处理并撤销凭证；只有重新认证且关系有效的监护人可发起；处理结果可解释并可追溯。

实施状态：已在本地完成，尚未部署。access/export/correct 的新建动作只受独立 `CHILD_DATA_RIGHTS_ENABLED` 控制，不依赖 Harmony 总门；withdraw/delete/terminate 安全入口和既有请求/回执读取在该门关闭时仍可用。跨家庭、同家庭无本人监护证据及其他监护人的请求统一隐藏资源存在性；相同幂等请求并发只产生一份请求与事件，冲突载荷稳定拒绝，中途故障完整回滚 reauth、幂等记录、别名、隐私状态和审计。授权撤回后本人仍可查阅、导出或发起删除；若已在 `deletion_pending`，后续撤回只撤销本人的 active 授权而不降级儿童状态。逐类留存表、备份同步清理和不可变账本去标识方案仍是生产硬门。

### S6：小程序监护端

提交建议按页面主题拆分：`feat(miniapp): add guardian consent flow`、`feat(miniapp): add family approval inbox`、`feat(miniapp): add child privacy center`。

最小顺序：请求基座 → 授权向导 → 设备页 → 家庭待办 → 家庭与隐私。授权前只允许读取公开法律文本；首轮只使用合成家庭，不开放普通用户。

实施状态：代码已在本地完成，尚未进行微信开发者工具编译/受控成人设备烟测，未部署、未开放普通用户。新增固定 HTTPS v2 传输、成人 Bearer 会话快照、稳定错误与密码学安全幂等键；公开法律文本匿名读取，其他监护接口从当前成人会话推导家庭和身份。授权页逐项冻结四类文本、关系声明与 reauth 证据；授权建档和配对生成在发请求前写入经读回验证的非秘密恢复标记，重启后只允许原键对账。设备页覆盖配对恢复、家长 challenge 确认、设备和会话撤销；家庭待办覆盖汇总、筛选、详情、退回、批准与驳回；家庭与隐私覆盖本人历史授权、撤回、查阅/导出、更正、删除与终止服务。

所有敏感页面订阅会话代次并在隐藏、卸载或账号切换时清理密码、reauth、短码、challenge 与导出正文；成人会话 A→B 先写入并验证空 Token 墓碑，再暂存用户并最后写新 Token，防止存储故障形成跨家庭旧 Token/新用户组合。授权、数据行权、积分审批、设备与会话 mutation 对成功 DTO 绑定原操作、目标资源、终态与 revision；畸形 2xx 按结果未知保留原键/意图。查阅/导出只在页面内存按白名单展示，不写文件、剪贴板或持久缓存。监护端不暴露孩子 claim、签名完成或 Refresh 能力。服务端补充本人历史监护儿童最小读取模型、授权操作恢复查询，以及配对创建在功能门关闭后的同键恢复；撤销 pending binding 会原子取消配对且重放不返秘密。

安全加固状态：小程序运行环境改为只认编译时 `envVersion`；`release` 精确绑定生产源，`develop`、`trial` 和未知环境在独立合成 API 获批前均于 `wx.request` 前以 `API_ENVIRONMENT_INVALID` 零联网拒绝，且不接受存储、ext config 或启动参数改写。跟踪项目配置强制 `urlCheck: true`，静态门检查生产源唯一声明、环境策略和 `web-view` 边界。普通行权现也在 POST 前持久化并回读不含正文、密码或 reauth 的恢复句柄，使用原幂等键通过新 operation 接口对账，再读取本人详情核对儿童和请求类型；结果未知、`pending`、`not_found`、清理失败或跨会话响应均持续阻断新键。401、隐藏、换账号、双重清理和撤回并发已做作用域与生命周期回归。

法律证据由服务端 `LEGAL_PUBLIC_ORIGIN` 和小程序当前环境共同约束，四类文本及监护关系声明只接受 `/legal/<固定类型>/<版本>/<小写 SHA-256>.html` 精确叶子；`web-view` 加载失败会清空 URL。设备页展示完整八组 SHA-256 指纹。最终 `npm test` 182/182、`npm run check` 和 `git diff --check` 通过，多路只读复核 P0/P1 为 0。阶段退出仍未完全满足：独立合成非生产 API 尚未建立，所以开发版/体验版被有意保持零联网；安全文件导出交付、微信开发者工具编译和受控成人设备 smoke 未完成，正式法律页面及重定向/CSP/微信业务域名也尚未发布验证。

## 9. 功能门与启用规则

默认值全部为 `false`：

- `harmony_child_enabled`
- `child_enrollment_enabled`
- `device_pairing_enabled`
- `point_requests_enabled`
- `child_data_rights_enabled`

当前服务端环境变量与逻辑门对应如下；只有 `1`、`true`、`yes`、`on`（不区分大小写）会开启，未设置或其他值均按关闭处理：

| 环境变量 | 用途 |
| --- | --- |
| `HARMONY_CHILD_ENABLED` | HarmonyOS/v2 儿童新流程总门；不控制共享旧家长端 |
| `CHILD_ENROLLMENT_ENABLED` | 监护授权与儿童建档 |
| `DEVICE_PAIRING_ENABLED` | 设备配对与设备会话 |
| `POINT_REQUESTS_ENABLED` | 积分申报审批 |
| `CHILD_DATA_RIGHTS_ENABLED` | 儿童数据行权流程 |
| `LEGACY_CHILD_LOGIN_ENABLED` | 仅用于显式兼容验证的旧儿童密码登录，生产保持关闭 |
| `LEGACY_CHILD_MANAGEMENT_ENABLED` | 仅用于显式兼容验证的旧儿童增删改，生产保持关闭 |

新流程开关不能连带开启任何 `LEGACY_*` 开关。

`CHILD_DATA_RIGHTS_ENABLED` 只门控新的 access/export/correct 请求；既有请求状态、回执和已授权导出保持可读，withdraw/delete/terminate 不受该门或 Harmony 总门封锁。删除/终止仍受动作专用重新认证、本人历史监护证据、revision、幂等和策略阻断作业约束，功能门关闭不等于放宽授权。

生产开启设备配对时必须同时明确客户端地址边界：直连部署使用 `PAIRING_CLIENT_IP_MODE=direct`；反向代理部署使用 `PAIRING_CLIENT_IP_MODE=trusted_proxy`，并在 `TRUSTED_PROXIES` 中配置实际可信代理 IP/CIDR。未声明时生产启动拒绝开启设备配对；边缘层仍需独立限流和容量保护。

法律文本服务必须显式配置不带路径、端口、用户信息、查询或片段的精确 HTTPS `LEGAL_PUBLIC_ORIGIN`，并让数据库公开 URL 和监护关系声明公开 URL 都匹配固定类型、版本和摘要叶子。配置缺失或任一证据不匹配时不发布当前法律文本，也不得开启授权建档。

用于设备凭据派生、本人流水及积分申报列表游标 AEAD 的服务端根密钥必须纳入生产密钥托管、轮换与单密钥签发量监测；上线前须明确轮换后旧凭据、旧游标的失效和客户端恢复策略。

共享旧家长端不读取 `HARMONY_CHILD_ENABLED`；它以 `child_privacy_states` 为全渠道合规真值门，并额外要求当前成人本人持有该儿童的 active 监护授权。这样关闭 HarmonyOS 新流程不会误伤已授权家长操作，而 suspended、processing_blocked 等状态仍会阻断所有客户端新增处理。

功能门是应急止损手段，不是绕过正式上架的私有分发方式。首次正式上架前，只有在总体计划的合规、迁移、隔离、删除、备份和发布硬门全部满足后，才可面向符合条件的普通家庭开启核心能力。

## 10. 测试与迁移门禁

新增测试文件建议：

- `server/test/v2-security-regression.test.js`
- `server/test/guardian-consents.test.js`
- `server/test/device-pairing-sessions.test.js`
- `server/test/point-requests.test.js`
- `server/test/data-rights.test.js`
- `server/test/stage1-migrations.test.js`
- `server/test/pre-migration-backup.test.js`
- `server/test/v2-http-errors.test.js`
- `server/test/miniapp-environment.test.js`
- `server/test/miniapp-hardening.test.js`

每个切片至少执行：

```bash
npm test
npm run check
git diff --check
```

迁移测试只使用临时数据库，覆盖 `005 → 006～009`、代码回退、再次前滚、`integrity_check`、备份哈希、家庭数/余额/流水核对。不得以真实数据库作为开发验证样本。

## 11. 当前明确不做

- 不恢复 AppGallery 未成年人邀请测试路线；
- 不向未成年人设备侧载测试包，不要求切换成人账号、关闭未成年人模式或开启开发者模式；
- 不读取或迁移真实儿童数据；
- 不实现照片、Push、广告、支付、第三方统计、用机兑换或时间兑换；
- 不部署、不开放生产功能、不 push；
- 不改父仓和现有远端双分支。

## 12. 下一项可直接实施的任务

先建立获批的独立合成非生产 API，再只通过受控编译时配置将 `develop`/`trial` 从当前零联网状态切到该源；不得允许预览访问生产 host 或使用运行时域名覆盖。随后在微信开发者工具和受控成人设备完成登录/退出、法律页、授权与普通行权恢复、配对确认/撤销、待办审批、前后台切换及账号切换 smoke，并保存不含个人信息和凭据的结果；同时完成安全文件导出交付/披露回执设计。代码侧可继续准备 S7 HarmonyOS 孩子端薄 MVP，只调用已经完成的设备配对、本人只读和积分申报接口。全过程继续只使用合成家庭；正式法律文本、PIPIA、存量数据整改、逐类留存/删除策略、受托方约束、密钥/备份演练和发布审查未完成前不得开启儿童生产功能门。
