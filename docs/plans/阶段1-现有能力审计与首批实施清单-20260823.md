# 阶段 1：现有能力审计与首批实施清单

- 日期：2026-08-23（2026-08-28 更新 S16 synthetic 外部签名束离线验证）
- 状态：S0、S1/006、S2/007、S3、S4/008、S5/009、S6 及其安全加固、S7 配对/会话/本人只读、S8 reward rules/文字申报/本人申请、S9 loopback 全链与受控临时 HarmonyOS profile、S10 本地设置与隐私安全壳、S11 小程序受控 synthetic 工作区、S12 合成运行配置与已提交预检门、S13 合成数据根离线 prepare/verify、S14 一次性安全 bootstrap、S15 候选机器证据离线绑定与未认证声明信封以及 S16 外部签名束离线验证已在本地主题分支完成，最终验证结果以 S16 交接为准；真实外部信任闭环仍未完成，未连接外部业务服务、未部署、未开放生产
- 基线：`codex/repo-baseline-20260820@921e32a`
- 审计分支：`codex/stage1-reuse-audit-20260823`
- S1 实现分支：`codex/stage1-s1-enrollment-20260823`
- S2 实现分支：`codex/stage1-s2-device-pairing-20260823`
- S3 实现分支：`codex/stage1-s3-child-self-views-20260823`
- S4 实现分支：`codex/stage1-s4-point-requests-20260823`
- S5 实现分支：`codex/stage1-s5-data-rights-20260823`
- S6 实现分支：`codex/stage1-s6-miniapp-guardian-20260824`
- S6 安全加固分支：`codex/stage1-s6-miniapp-hardening-20260824`
- S7 实现分支：`codex/stage1-s7-harmonyos-child-mvp-20260825`
- S8 实现分支：`codex/stage1-s8-harmonyos-point-requests-20260825`
- S9 实现分支：`codex/stage1-s9-synthetic-e2e-20260825`
- S10 实现分支：`codex/stage1-s10-child-privacy-settings-20260825`
- S11 实现分支：`codex/stage1-s11-miniapp-synthetic-profile-20260826`
- S12 实现分支：`codex/stage1-s12-synthetic-runtime-safety-20260826`
- S13 实现分支：`codex/stage1-s13-synthetic-data-root-readiness-20260827`
- S14 实现分支：`codex/stage1-s14-synthetic-bootstrap-20260827`
- S15 实现分支：`codex/stage1-s15-synthetic-candidate-evidence-20260828`
- S16 实现分支：`codex/stage1-s16-external-approval-verification-20260828`
- 总体依据：[糖罐积分 HarmonyOS 版 MVP 方案与推进计划](糖罐积分鸿蒙版-MVP方案与推进计划.md)

## 1. 结论

现有 Express、SQLite 和微信小程序可以作为阶段 1 的工程底座，不需要重写。家庭隔离、积分账户与流水、稳定规则 ID、规则版本、事务封装、`revision` 条件写、临时数据库测试和小程序统一请求封装都可以复用。

但是，现有儿童账号、旧 Token、积分与历史查询、儿童创建和数据清理接口不能直接供 HarmonyOS 儿童端使用。它们目前不满足监护人授权、孩子本人最小可见、设备绑定、会话轮换、审批幂等和可审计数据行权等要求。

阶段 1 必须先完成安全前置，再依次实现“授权建档 → 设备配对 → 本人查询 → 积分申报审批 → 数据行权”。全部儿童生产功能默认关闭，只允许用合成家庭验证；本文件不代表已经对真实儿童数据完成盘点、取得授权或开放生产。

## 2. 审计边界

本轮只读取源码、工程文档和合成测试，不读取 `data/`、`data-dev/`、`backups/`、日志、真实账号、签名材料或发布凭据，不修改远端和父仓。

验证结果：

- 审计基线 `npm test`：29/29 通过；
- S2/007 完成后 `npm test`：86/86 通过；
- S3 完成后 `npm test`：94/94 通过；
- S5/009 完成后 `npm test`：119/119 通过；
- S6 完成后 `npm test`：156/156 通过；
- S6 安全加固后 `npm test`：182/182 通过；
- S7 HarmonyOS 切片后 `npm test`：189/189 通过；
- S7 unsigned ArkTS Hypium：17/17 通过；HarmonyOS 静态安全门：7/7 通过；ArkTS CodeLinter：0 缺陷；
- S8 HarmonyOS 积分申报切片后 `npm test`：191/191 通过；
- S8 unsigned ArkTS Hypium：38/38 通过；HarmonyOS 静态安全门：8/8 通过；ArkTS CodeLinter：0 缺陷；
- S9 合成 E2E 就绪切片后 `npm test`：199/199 通过；S9 定向回归：16/16 通过；
- S9 干净来源临时 unsigned profile：Hypium 39/39 通过；HarmonyOS 静态安全门：10/10 通过；CodeLinter 因本机 CLI 无法解析已安装 SDK 未计入有效新证据；
- S10 本地设置与隐私安全壳后 `npm test`：200/200 通过；HarmonyOS 静态安全门：11/11 通过；
- S10 ArkTS 变更后的临时 unsigned 工程 BUILD SUCCESSFUL、Hypium 43/43；CodeLinter 0 error、0 warning、1 条既有 1024×1024 启动图标尺寸建议；双重最终复核无 P0/P1；
- S11 `npm test`：212/212 通过；小程序环境/安全门/synthetic 定向 28/28、`npm run check`、`git diff --check` 通过；三路最终只读复核无剩余 P0/P1；
- S12 `npm test`：236/236 通过；服务端 synthetic 配置定向 21/21、S12 客户端来源与服务端配置三文件定向 34/34、`npm run verify:synthetic-api-preflight`、`npm run check`、`git diff --check` 通过；
- S13 数据根专测 14/14、与 S12 配置门合并定向 35/35、根回归 250/250、已提交离线预检与 `npm run check` 通过；并发 prepare 用例额外重复 20 次均保持单一成功者；
- S14 bootstrap 专测 16/16、S12～S14 synthetic 定向 53/53、根回归 268/268、已提交离线预检、`npm run check` 与 `git diff --check` 通过；所有用例只使用合成配置、临时目录与临时 SQLite；
- S15 bootstrap/candidate/data-root/config 定向 66/66、根回归 281/281、已提交离线预检通过；全部只使用合成配置、系统临时目录与临时 SQLite，未连接外部服务或生产资源；
- S16 external-approval 专测 19/19、bootstrap/candidate/data-root/config/external-approval 定向 85/85、根回归 300/300、已提交离线预检、`npm run check` 与 `git diff --check` 通过；真实成功 CLI 回归只使用动态合成时间、内存 Ed25519 密钥、系统临时目录和临时 SQLite，并确认只读、单行脱敏与始终不授权；
- `npm run check`：通过；
- 审计开始时工作树干净。

存量儿童数据的合法性、保留依据、备份同步删除和 PIPIA 仍是独立合规工作流，不能用本次代码审计替代。

## 3. 可直接复用的能力

### 3.1 后端

| 能力 | 现状 | 阶段 1 用法 |
| --- | --- | --- |
| Express 模块化路由 | 入口可挂载独立路由 | 新增 `/api/v2`，不改变旧家长端接口语义 |
| SQLite 增量迁移 | 有迁移登记、WAL、`BEGIN IMMEDIATE` 和统一事务 | 继续使用独立编号迁移；补迁移前备份和前滚/代码回退测试 |
| 家庭、用户、积分账户和流水 | 已有基础表和家庭作用域 | 作为账本底座；儿童设备端只能走本人专用接口 |
| 积分原子入账 | 余额更新和流水插入在同一事务 | 抽出可接收现有事务的底层函数，供审批事务调用 |
| 稳定规则/分类 ID | 服务端已有 ID、分值校验和名称快照 | 申报保存规则快照，不能仅保存可变名称 |
| `revision` 条件写 | 规则并发修改已有“一成一败”实现 | 复用到申请审批、授权和设备确认 |
| 测试框架 | 临时 SQLite、合成家庭、临时 HTTP 服务和原生 `fetch` | 新测试继续禁止连接真实数据 |
| 请求 ID、结构化日志和外围限流 | 已有通用中间件 | 继续使用；儿童审计日志改为字段白名单，短码锁定必须落库 |

### 3.2 微信小程序

| 能力 | 现状 | 阶段 1/2 用法 |
| --- | --- | --- |
| `fetchAPI` 统一请求 | 已集中封装 Token 与错误处理 | 扩展 v2 错误码、幂等键、`expectedRevision`、短期重新认证凭据 |
| 家庭会话与孩子卡片 | 已有登录态、家庭作用域和孩子选择器 | 复用为授权后选择孩子、创建设备配对任务 |
| 记录列表视觉结构 | 已有筛选、分组和详情弹窗 | 复用列表样式；申请审批必须使用独立数据模型和详情组件 |
| 分值调整弹窗 | 已有数字与备注输入 | 复用为家长审批时调整批准分值 |
| “我的”页面 | 已有常驻入口 | 增加“家庭与隐私”：授权、设备、数据权利和处理回执 |
| 家庭作用域本地存储 | 已有分家庭 key 设计 | 只保存授权前本地孩子别名草稿，不得提前同步儿童业务数据 |

## 4. 必须先处理的 P0 问题

### P0-1：未登录可枚举全部家庭成员

`GET /api/config` 在没有 Token 时调用 `users.listAll()`，可能返回所有家庭成员的 ID、姓名、角色和家庭 ID。阶段 1 的第一项代码改动必须移除匿名成员列表；小程序未登录首页不得再依赖该响应展示用户。

验收：匿名调用不返回任何用户、孩子或家庭关联；旧家长登录方式仍可用；有安全回归测试。

### P0-2：现有儿童创建绕过监护人授权

`POST /api/family/child/create` 可直接创建孩子，且缺省密码为 `123456`；`/api/config/users` 也可以批量改写儿童。家庭管理员身份不等于已经验证的法定监护关系。

验收：旧儿童新增能力受默认关闭的服务端开关约束；新孩子只能通过原子的 `POST /api/v2/child-enrollments` 创建。事务必须同时验证短期重新认证、关系声明、三份公开文本和敏感信息单独告知同意，任一失败均不得产生孩子或授权记录。禁止固定默认密码。

### P0-3：儿童旧 Token 可读取兄弟姐妹数据

`GET /api/points` 会返回全家庭积分账户，`GET /api/history` 在未提供或提供无效 `kid` 时可能退化为全家庭流水。

验收：旧接口对 `child` 角色收窄并有回归测试；HarmonyOS 只使用 `/api/v2/me/summary`、`/api/v2/me/transactions`、`/api/v2/me/reward-rules` 和 `/api/v2/me/point-requests` 等固定设备接口。`childId` 必须从设备会话推导，客户端不能指定其他孩子。

实施状态：已完成 S3 本人只读切片。两个 `/api/v2/me` 接口严格只接受设备 Access Bearer；家庭、儿童、绑定和会话均来自认证上下文，业务查询再次复验 active session、binding、同一监护人的当前 active 授权、隐私状态、儿童角色以及 `pointsLedger/self_only` 范围。绑定仍保留创建时授权版本作为签发审计证据；正常续签后既有设备继续可用，当前授权撤回后立即失效。摘要不接受查询参数；流水只接受有上限的 `limit` 和作用域绑定的 AEAD 不透明游标，软删除、兄弟姐妹、跨家庭及内部字段不会进入响应。

### P0-4：当前备份发生在迁移之后

数据库连接会自动执行迁移，而现有备份脚本通过同一连接打开数据库，所以不能满足“旧库备份 → 校验 → 迁移”。旧 JSON 迁移脚本还在使用已经过时的规则表写法。

验收：提供不加载应用连接模块的迁移前备份流程，先复制数据库及 WAL、生成 SHA-256、执行 `PRAGMA integrity_check`，再启动兼容代码执行迁移；旧 JSON 迁移脚本修复并以临时库测试，或明确归档为不可用。

实施状态：已完成。`scripts/pre-migration-backup.js` 不加载应用连接，使用 SQLite `VACUUM INTO` 生成包含已提交 WAL 内容的一致性快照，并生成文件 SHA-256、全库逻辑内容 SHA-256、完整性、外键、迁移集合与业务计数清单。生产连接在有存量数据和待迁移版本时必须校验 `PRE_MIGRATION_BACKUP_MANIFEST`，并重算当前源库逻辑摘要；缺失、陈旧或不匹配时拒绝迁移。旧 JSON 迁移脚本的单独归档/重写仍留作后续历史清理，不用于 006 发布。

### P0-5：旧会话不能作为儿童设备会话

当前 Token 最长 30 天，自定义 HMAC 签名只截取 16 个十六进制字符，支持从 query/body 读取，没有设备绑定、短期 Access Token、Refresh 轮换、重用检测或持久会话。`/wx-bind` 还信任客户端提交的 `openid`。

验收：v2 只接受 `Authorization`；设备 Token 固定绑定 `familyId + childId + deviceBindingId`；Access 短期、Refresh 摘要落库并单次轮换；旧 Refresh 重用撤销整组；撤销设备或授权后立即失效；`openid` 只能由服务端换码得到。

实施状态：已完成。设备 claim、Access 和 Refresh 严格只接受 Bearer；Access 为 15 分钟，Refresh 为 30 天且只在 Access 剩余 5 分钟内签发单个 pending proof challenge。Refresh 按 token family 单次轮换，旧凭据非精确重试或超过固定幂等宽限后会撤销整组。设备、会话、授权撤回和儿童处理阻断均在持久状态上即时生效。

## 5. 不得误复用的旧能力

- 微信登录页的“同意授权”只是平台登录提示，不是监护人同意证据，必须改成准确文案。
- 家庭 6 位邀请码有效期和重用模型不同，不能复用为设备配对码。
- 家长直接调用 `/api/points/change` 的随手记能力不能作为儿童申报审批。
- `/api/history/delete` 仅隐藏流水且不冲正余额，不能删除审批流水；更正必须新增反向流水。
- 管理员“删除用户”和“清理历史”不能作为儿童查阅、删除或终止服务流程。
- 家庭 `admin/parent` 角色不能直接等同于已声明并验证的监护人。
- 现有内存限流重启后丢失，只能作为外围保护；短码失败计数和锁定必须持久化。

## 6. 迁移顺序

实际编号以当前 `005` 后续为准，每批独立迁移、独立测试，生产回滚采用“关闭功能开关 + 回退代码 + 保留新表数据”，不得直接删除授权、申请或审计数据。

### 006：授权与儿童建档

建议文件：`006_guardian_consent_enrollment.sql`

- `legal_text_versions`：四类服务端认可文本的版本、SHA-256、公开 URL 和生效时间；
- `reauth_assertions`：绑定用户、家庭和用途的 5 分钟一次性断言，只保存随机令牌摘要；
- `child_privacy_states`：`suspended_pending_consent`、`active`、`processing_blocked`、`deletion_pending`、`deidentified`、`deleted`；未授权孩子别名只允许保存在家长端本地草稿中，服务端不建草稿；
- `guardian_consents`：不可变授权版本、监护关系声明版本、四类文本版本与摘要、授权范围、撤回与替代关系；
- 新建孩子的密码登录固定关闭，孩子身份只能来自后续设备绑定；
- 存量儿童默认进入 `suspended_pending_consent`，在完成独立合规盘点前不得自动激活。

### 007：设备配对与会话

建议文件：`007_device_pairing_sessions.sql`

- `pairing_challenges`：短期、单次、失败次数、锁定和状态；
- `device_bindings`：随机公开 ID、公钥、孩子和有效授权版本；
- `device_sessions`：Access/Refresh 生命周期、轮换组和撤销；
- `device_session_challenges`：签名 challenge 单次消费，防重放。

数据库只保存设备配对短码的带密钥 HMAC 索引，不保存明文；配对申领只返回不透明 claim ID。

实施状态：已完成。短码和持久失败主体使用分域 HMAC，claim、Access、Refresh 和 proof challenge 只落摘要；家长须使用独立高熵 challenge 和 `expectedRevision` 二次确认。设备使用 canonical P-256 SPKI 对分域 signing payload 签名，只有 proof 成功后设备身份才取得 active 唯一性，避免公开身份在 proof 前被抢注。授权撤回、设备撤销、会话撤销、配对/证明过期或锁定均通过事务和数据库触发器撤销相关安全产物。

### 008：积分申报与来源唯一性

建议文件：`008_point_requests_transaction_sources.sql`

- `point_requests`：状态、`revision`、规则快照、申报分值、批准分值、说明、客户端请求 ID；
- `transactions.source_type/source_id`；
- `UNIQUE(device_binding_id, client_request_id)`；
- 同一家庭同一业务来源只允许一条有效流水的唯一约束；
- 家庭待办、孩子本人状态和疑似周期重复索引。

实施状态：已在本地完成，尚未部署。设备只可提交当前家庭 reward 规则，服务端保存规则版本、名称和范围快照；同一绑定 `clientRequestId` 与请求指纹共同实现跨重启领域幂等，疑似同周期重复只标记不丢弃。申请转换有 revision、不可变事件和幂等摘要证据；成人每次列表或审批均复验本人当前监护授权范围。批准、既有账户更新和唯一来源流水共用 `BEGIN IMMEDIATE`，带来源流水由数据库禁止改写、软删或硬删。授权撤回后待审证据保留，但设备会话撤销且隐私阻断使任何后续处理无法发生。迁移与 API 回归仅使用临时 SQLite 和合成家庭。

### 009：数据行权与审计

建议文件：`009_data_rights_audit.sql`

- `data_rights_requests`；
- 字段白名单 `audit_events`；
- 如删除异步执行，增加 `data_deletion_jobs`；
- 撤回授权事务同步暂停儿童处理、撤销设备绑定和所有设备会话。

实施状态：已在本地完成，尚未部署。唯一创建入口按儿童资源提供 `access/export/correct/delete/terminate`；既有授权撤回入口在同一事务补写 009 请求和不可变事件。请求绑定当前家庭成人本人最新的 active/withdrawn/superseded 监护证据、动作专用已消费 reauth、revision 和持久幂等摘要；列表使用绑定家庭、主体与筛选条件的 AEAD keyset 游标。导出只凭短时有效的已完成 access/export 请求动态生成，不持久化导出正文，并排除密码、密钥、公钥、凭据/幂等摘要和 challenge。首版更正只更新当前儿童别名，历史积分流水、申请、事件和规则/别名快照均保持不可变。删除或终止只原子进入 `deletion_pending`、撤销目标儿童全部设备安全产物和旧 Token、生成永久锁定为 `blocked_policy/policy_pending` 的作业；正式留存决定获批前数据库禁止进入 completed/deidentified/deleted。迁移与 API 回归只使用临时 SQLite 和合成家庭。

首版不加入照片、Push、用机兑换、第三方统计、`time_redemptions` 或通知 Outbox。

## 7. v2 API 完整清单

### 公共法律文本与重新认证

- `GET /api/v2/legal-texts/current`
- `POST /api/v2/reauth-assertions`

### 授权和儿童建档

- `POST /api/v2/child-enrollments`
- `GET /api/v2/children/:id/consents`
- `POST /api/v2/children/:id/consents`
- `POST /api/v2/children/:id/consents/withdraw`

### 设备

- `POST /api/v2/device-pairings`
- `GET /api/v2/device-pairings/:id`
- `POST /api/v2/device-pairings/claim-by-code`
- `POST /api/v2/device-pairings/:id/confirm`
- `POST /api/v2/device-pairings/claim/complete`
- `GET /api/v2/devices`
- `DELETE /api/v2/devices/:id`
- `POST /api/v2/devices/:id/session-challenges`
- `POST /api/v2/device-sessions/refresh`
- `DELETE /api/v2/device-sessions/:id`

### 孩子本人和积分申报

- `GET /api/v2/me/summary`
- `GET /api/v2/me/transactions`
- `GET /api/v2/me/reward-rules`
- `GET /api/v2/me/point-requests`
- `POST /api/v2/point-requests`
- `GET /api/v2/point-requests/:id`
- `PATCH /api/v2/point-requests/:id`
- `POST /api/v2/point-requests/:id/request-info`
- `POST /api/v2/point-requests/:id/approve`
- `POST /api/v2/point-requests/:id/reject`
- `POST /api/v2/point-requests/:id/cancel`

### 数据权利

- `GET /api/v2/data-rights-operations/request-create`
- `GET /api/v2/data-rights-requests`
- `GET /api/v2/data-rights-requests/:id`
- `GET /api/v2/children/:id/data-export`
- `POST /api/v2/children/:id/data-rights-requests`

所有状态变更请求必须携带幂等键；涉及并发决定时携带 `expectedRevision`。统一返回稳定错误码，至少包括 `FEATURE_DISABLED`、`CONSENT_REQUIRED`、`REAUTH_REQUIRED`、`FORBIDDEN_SCOPE`、`VALIDATION_ERROR`、`CHILD_DATA_INCOMPLETE`、`REVISION_CONFLICT`、`IDEMPOTENCY_CONFLICT`、`PAIRING_EXPIRED`、`PAIRING_LOCKED` 和 `SESSION_REVOKED`。

## 8. 纵向实施切片与本地提交边界

每个切片独立提交、独立测试，不把整个阶段 1 塞入一个大提交。

### S0：安全前置

提交建议：`fix(server): close legacy child data exposure`

- 匿名 `/api/config` 不再返回成员；
- 旧儿童新增入口置于默认关闭开关；
- 旧 `child` 积分/历史查询限制为本人；
- `/api/v2` 鉴权上下文、稳定错误结构和全局开关骨架；
- 微信换码绑定不再信任客户端 `openid`；
- 新增 `v2-security-regression.test.js`。

退出条件：未登录、跨家庭和同家庭跨孩子越权测试全部通过，旧家长端回归通过。

### S1：授权建档

提交建议：`feat(server): add guardian consent enrollment`

- 006 迁移、法律文本、一次性重新认证、仓储、服务和 API；
- 原子建档；
- 存量儿童暂停状态；
- 不可变授权版本；
- `guardian-consents.test.js` 和迁移测试。

退出条件：拒绝任一文本或缺少重新认证均不建档；家庭管理员不会自动成为监护人；撤回立即阻止新增处理。

实施状态：已在本地完成，尚未部署。006 不预置伪造法律文本；四类文本和监护关系声明必须提供版本、SHA-256 与公开地址。建档、零余额账户、授权、隐私状态和幂等记录同事务完成。存量儿童回填为暂停态；旧成人接口只允许本人有效授权且状态 active 的儿童。任一监护人撤回会全局暂停，其他监护人不能覆盖；状态漂移后的旧幂等键返回 `IDEMPOTENCY_REPLAY_STALE`。

### S2：设备配对与会话

提交建议：`feat(server): add child device pairing sessions`

- 007 迁移和 API；
- 短码 HMAC、持久失败锁定、家长二次确认、公钥 challenge；
- Access/Refresh 轮换与撤销；
- `device-pairing-sessions.test.js`。

退出条件：过期、重放、猜码、跨家庭确认、旧 Refresh 重用和撤销后继续访问全部失败。

实施状态：已在本地完成，尚未部署。007 前滚保留既有业务与授权数据；家庭/儿童/设备复合外键、状态触发器、active 身份唯一性、会话代际和 challenge 结果链路均由数据库复核。API 覆盖创建、申领、家长确认、proof 完成、设备/会话列举与撤销、Refresh challenge 和轮换；稳定错误覆盖过期、锁定、作用域、revision、幂等冲突、提前刷新、凭据重用和撤销。所有测试仅使用合成家庭与临时 SQLite。

### S3：孩子本人只读

提交建议：`feat(server): add child self-only views`

- `/me/summary`、`/me/transactions`；
- 只返回当前绑定孩子；
- 关闭功能门时不处理儿童业务数据。

退出条件：孩子 A 无法通过参数、ID 或错误回退读取孩子 B。

实施状态：已在本地完成，尚未部署。摘要只返回本人 ID、别名和余额；流水按插入顺序使用 `limit + 1` 的 rowid keyset 分页，服务端将边界与交易 ID 摘要封装进绑定家庭、儿童和设备作用域的 AEAD 不透明游标，响应只返回业务 transaction ID、发生时间、分值、原因快照及可空规则/分类 ID。客户端身份选择、GET body、未知查询参数、畸形或跨绑定游标均稳定拒绝；设备、会话或授权撤销后旧 Access 立即失效，关闭 Harmony/Device 任一功能门时在业务查询前止损。所有回归只使用合成家庭和临时 SQLite。

### S4：积分申报审批

提交建议：`feat(server): add point request approval ledger`

- 008 迁移、状态机和家庭待办 API；
- `pending → needs_info → pending/approved/rejected/cancelled`；
- 审批和积分入账在同一数据库事务；
- `point-requests.test.js`。

退出条件：同一 `clientRequestId` 并发 100 次只产生一条申请；双亲同 revision 审批仅一次成功；未批准不入账；同一申请只产生一条来源流水。

实施状态：已在本地完成，尚未部署。已提供孩子提交、本人列表、补充、取消，监护人待办/详情、退回、批准、驳回及家庭待办汇总；设备和成人列表用 `limit + 1` keyset 与绑定家庭、主体、儿童筛选和状态的 AEAD 不透明游标分页，跨设备共享视图仅向来源绑定回显客户端请求 ID。规则变更后仍按提交快照审批；普通补报期限待确认，当前 fail-closed 为上海当日。100 次相同提交只产生一条申请，双监护人同 revision 只允许一次批准，故障注入会完整回滚申请、事件、余额和流水，缺失积分账户不会被自动补建。数据库迁移测试覆盖 007→008 数据保留、家庭/设备复合约束、状态机、来源完整性、旧流水兼容、重复迁移及 `integrity_check`/`foreign_key_check`。

### S5：数据行权

提交建议：`feat(server): add child data rights workflow`

- 009 迁移和可审计状态机；
- 查阅/导出、更正、撤回、删除和终止服务；
- 保留决定、删除作业和回执；
- `data-rights.test.js`。

退出条件：撤回立即停止新增处理并撤销凭证；只有重新认证且关系有效的监护人可发起；处理结果可解释并可追溯。

实施状态：已在本地完成，尚未部署。access/export/correct 的新建动作只受独立 `CHILD_DATA_RIGHTS_ENABLED` 控制，不依赖 Harmony 总门；withdraw/delete/terminate 安全入口和既有请求/回执读取在该门关闭时仍可用。跨家庭、同家庭无本人监护证据及其他监护人的请求统一隐藏资源存在性；相同幂等请求并发只产生一份请求与事件，冲突载荷稳定拒绝，中途故障完整回滚 reauth、幂等记录、别名、隐私状态和审计。授权撤回后本人仍可查阅、导出或发起删除；若已在 `deletion_pending`，后续撤回只撤销本人的 active 授权而不降级儿童状态。逐类留存表、备份同步清理和不可变账本去标识方案仍是生产硬门。

### S6：小程序监护端

提交建议按页面主题拆分：`feat(miniapp): add guardian consent flow`、`feat(miniapp): add family approval inbox`、`feat(miniapp): add child privacy center`。

最小顺序：请求基座 → 授权向导 → 设备页 → 家庭待办 → 家庭与隐私。授权前只允许读取公开法律文本；首轮只使用合成家庭，不开放普通用户。

实施状态：代码已在本地完成，尚未进行微信开发者工具编译/受控成人设备烟测，未部署、未开放普通用户。新增固定 HTTPS v2 传输、成人 Bearer 会话快照、稳定错误与密码学安全幂等键；公开法律文本匿名读取，其他监护接口从当前成人会话推导家庭和身份。授权页逐项冻结四类文本、关系声明与 reauth 证据；授权建档和配对生成在发请求前写入经读回验证的非秘密恢复标记，重启后只允许原键对账。设备页覆盖配对恢复、家长 challenge 确认、设备和会话撤销；家庭待办覆盖汇总、筛选、详情、退回、批准与驳回；家庭与隐私覆盖本人历史授权、撤回、查阅/导出、更正、删除与终止服务。

所有敏感页面订阅会话代次并在隐藏、卸载或账号切换时清理密码、reauth、短码、challenge 与导出正文；成人会话 A→B 先写入并验证空 Token 墓碑，再暂存用户并最后写新 Token，防止存储故障形成跨家庭旧 Token/新用户组合。授权、数据行权、积分审批、设备与会话 mutation 对成功 DTO 绑定原操作、目标资源、终态与 revision；畸形 2xx 按结果未知保留原键/意图。查阅/导出只在页面内存按白名单展示，不写文件、剪贴板或持久缓存。监护端不暴露孩子 claim、签名完成或 Refresh 能力。服务端补充本人历史监护儿童最小读取模型、授权操作恢复查询，以及配对创建在功能门关闭后的同键恢复；撤销 pending binding 会原子取消配对且重放不返秘密。

安全加固状态：小程序运行环境改为只认编译时 `envVersion`；`release` 精确绑定生产源，`develop`、`trial` 和未知环境在独立合成 API 获批前均于 `wx.request` 前以 `API_ENVIRONMENT_INVALID` 零联网拒绝，且不接受存储、ext config 或启动参数改写。跟踪项目配置强制 `urlCheck: true`，静态门检查生产源唯一声明、环境策略和 `web-view` 边界。普通行权现也在 POST 前持久化并回读不含正文、密码或 reauth 的恢复句柄，使用原幂等键通过新 operation 接口对账，再读取本人详情核对儿童和请求类型；结果未知、`pending`、`not_found`、清理失败或跨会话响应均持续阻断新键。401、隐藏、换账号、双重清理和撤回并发已做作用域与生命周期回归。

法律证据由服务端 `LEGAL_PUBLIC_ORIGIN` 和小程序当前环境共同约束，四类文本及监护关系声明只接受 `/legal/<固定类型>/<版本>/<小写 SHA-256>.html` 精确叶子；`web-view` 加载失败会清空 URL。设备页展示完整八组 SHA-256 指纹。最终 `npm test` 182/182、`npm run check` 和 `git diff --check` 通过，多路只读复核 P0/P1 为 0。阶段退出仍未完全满足：独立合成非生产 API 尚未建立，所以开发版/体验版被有意保持零联网；安全文件导出交付、微信开发者工具编译和受控成人设备 smoke 未完成，正式法律页面及重定向/CSP/微信业务域名也尚未发布验证。

### S7：HarmonyOS 儿童端安全纵向切片

本切片复用已经完成的 S2/007 设备配对与会话、S3 本人只读服务端契约，只实现儿童端的配对、会话恢复、余额和流水。它不宣称完成总体计划阶段 3 的文字申报、我的申请、隐私摘要、设置、审核材料或端到端成人预验收。

实施状态：`0.2.0 (20000)` 客户端已在本地完成，未联网、未打包签名、未部署。跟踪环境固定 `NETWORK_ENABLED=false` 和 `.invalid` 源；transport 只允许 claim-by-code、claim complete、设备 Refresh challenge、Refresh 轮换、本人摘要和本人流水，禁止请求携带客户端选择的家庭、儿童、设备或会话身份。UI 覆盖 6 位短码、等待家长二次确认、结果未知恢复、会话恢复、本人余额和分页流水；进入后台即清空内存业务视图。

设备私钥使用 HUKS P-256 / SHA-256、`KEY_OVERRIDE=false` 和解锁态 ECE 存储，公钥导出前核验 canonical P-256 SPKI。Access/Refresh、固定 Refresh 截止和写前持久化意图使用 AssetStore `DEVICE_UNLOCKED`、禁止同步与持久备份、写后读回验证；本地 envelope 按相位互斥，结果未知只能重放原 claim/旧 Refresh、challenge、签名和幂等键。服务端明确返回 challenge 过期后才生成新的 Refresh issue 键；授权撤回、设备或会话撤销会在服务端立即使凭据失效，客户端在下一次请求收到确定性撤销响应后清除本地 Token 并回到未配对。儿童处理阻断会立即拒绝业务处理并清内存视图，但保留会话和待恢复意图。备份 Extension、动态敏感日志、Preferences/文件/剪贴板凭据和硬件设备标识均未使用。

验证只使用合成对象、临时 SQLite 和 unsigned 本地构建：`npm test` 189/189、`npm run check`、`git diff --check` 通过；Hypium 17/17，覆盖完整配对、完整 Refresh 轮换、六条 wire contract、畸形 2xx、跨重启 exact replay、过期恢复、撤销和相位污染；CodeLinter 0 缺陷，多路最终安全复核无 P0/P1。未执行 HUKS/AssetStore 真机验证、联网端到端 smoke、HAP/APP 打包、签名或 AppGallery 操作。

S7 的停止点是：现有服务端可以接收设备作用域 point request，却没有让设备安全读取当前家庭 reward rules 的最小 API。客户端因此没有硬编码规则、读取旧共享配置或调用成人规则接口，缺口留给下一独立切片。

### S8：HarmonyOS 积分申报纵向切片

本切片补齐 `GET /api/v2/me/reward-rules`，并在 HarmonyOS 端实现当前可申报规则、文字积分申报和“我的申请”。它继续复用 S2/007 设备身份和 S4/008 申请账本，不引入儿童可选家庭/孩子/设备，不复用成人 `GET /api/v2/point-requests`，也不实现照片、Push、用机兑换、申请补充、取消或重新提交。

服务端只从当前有效设备 Access 上下文返回当前家庭 reward 规则的最小规范化快照，过滤不可申报项，不返回 punish/special、别名、提示、家庭、儿童或内部规则标识。分页游标使用独立用途派生的 AEAD，绑定家庭、儿童、设备、规则 revision 和快照指纹；跨家庭、跨孩子、同一孩子的另一设备、篡改或规则变更都稳定拒绝。规则列表和创建申请共用同一规范化当前快照，避免展示值与提交校验分叉。授权撤回、儿童处理阻断、设备/会话撤销以及功能门关闭仍会在读取或写入前 fail closed。

客户端为 reward rules、申请创建和本人申请列表建立固定 method/path/credential/body allowlist 和严格 DTO 校验。UI 只允许选择服务端规则范围内积分并提交不超过 120 字的单行说明，显示本人申请状态并支持分页；后台、会话切换、撤权和隐私阻断都会清除内存规则、申请与草稿。所有写操作先把规范化请求体、原幂等键和绑定 ID 写入独立 AssetStore 并读回验证；结果未知或重启时先在本人最近申请中按完整快照对账，未找到才原样重放。幂等冲突、结果不可用或本地清理失败继续锁定原意图，避免生成第二个键；撤权时先清申请意图，再清设备会话。

验证只使用合成对象、临时 SQLite 和 unsigned 本地构建：`npm test` 191/191、`npm run check`、`git diff --check` 通过；Hypium 38/38，HarmonyOS 静态安全门 8/8，CodeLinter 0 缺陷，多路最终只读安全复核无 P0/P1。未执行联网、HUKS/AssetStore 真机验证、HAP/APP 打包、签名、部署或 AppGallery 操作，跟踪配置仍固定 `NETWORK_ENABLED=false` 和 `.invalid` 源。

申请补充、取消和重新提交仍有意暂缓：当前设备契约没有单条详情和操作结果对账，无法在网络结果未知后证明精确结果。未决文字意图还需要生产保留期限和监护人授权的安全放弃策略；在这些边界获批前不得用本地猜测清理或增加新的写路径。

### S9：合成端到端就绪切片

本切片没有建立外部环境，也没有开启跟踪配置网络。它先使用生产 `server/index.js#createApp`、`127.0.0.1:0`、合成家庭和临时 SQLite 跑通真实 HTTP 中间件与路由：成人登录、法律证据、重新认证、授权建档、P-256 设备 proof、短码申领、家长二次确认、设备 Access/Refresh、本人摘要/规则、积分申报、家长审批入账、响应丢失后的原键恢复、跨家庭隐藏、Refresh 轮换和撤回授权联动撤销。监听器重启只保留临时数据库，原 `clientRequestId` 和幂等键恢复后仍只有一条申请；外家庭详情和审批稳定返回 `POINT_REQUEST_NOT_FOUND`，撤权后新 Access 与 Refresh 都稳定返回 `SESSION_REVOKED`。

HarmonyOS 新增严格的 canonical HTTPS origin 校验和 `npm run prepare:harmonyos-synthetic`。生成器只接受显式确认的非生产公共 HTTPS origin，只能在经校验的本机系统临时目录创建新副本；拒绝生产域、IP、HTTP、端口、路径、查询、用户信息、UNC、仓库内/非临时输出、reparse 父目录、非普通 Git 文件、符号链接、子模块和签名类扩展。它只读取 Git 跟踪的 HarmonyOS 普通文件，不读取被忽略的私有根构建配置；生成空签名配置，并记录 HEAD、源树 SHA-256、dirty 状态和前后稳定性。CLI 成功/失败都不回显 origin 或本机路径，测试通过 preload 阻断 Node 网络 API，命令自身不连接给定源。

验证只使用合成对象、临时 SQLite 和系统临时目录：`npm test` 199/199、S9 定向 16/16、`npm run check`、`git diff --check` 通过；来自干净提交的临时 unsigned profile BUILD SUCCESSFUL，Hypium 39/39；最终只读安全复核无剩余 P0/P1。CodeLinter 本轮未计为有效证据，因为本机 CLI 报告无法解析已安装 HarmonyOS SDK；S8 的 0 缺陷结果不能替代发布候选重跑。未执行外部联网、HUKS/AssetStore 模拟器或真机验证、HAP/APP 打包、签名、部署或 AppGallery 操作，跟踪配置仍固定 `NETWORK_ENABLED=false` 和 `https://harmony-child.invalid`。

S9 的停止点是外部基础设施和受控设备：需要先由项目批准并建立物理、域名、凭据和数据均独立的 synthetic API，配置 DNS/TLS、独立密钥、受控部署与合成家庭；随后才能使用临时 profile 在模拟器或成人受控设备执行联网 smoke。生成器可编译不等于 endpoint 已存在，更不等于儿童可安装或生产功能可开启。

### S10：HarmonyOS 儿童设置与隐私安全壳

本切片只在现有 `Index` 单页中加入条件视图，没有新增路由。顶部“设置与隐私”入口切换前会清除配对短码和积分草稿；进入后台时复位安全壳，并继续执行既有页面锁定和业务视图清理。安全壳只从固定本地模型展示版本、通用设备别名、受控网络状态、后台隐藏、通知/照片和备份关闭状态，以及数据类别、最小可见范围、家长端操作提示和生产硬门；不读取动态儿童、家庭、设备、绑定、会话或凭据。

页面显著声明它只是开发阶段工程安全说明，不是正式《隐私政策》《儿童个人信息保护规则》《儿童用户协议》或同意页面。它不联网、不导航、不持久化、不提供假解绑、撤权、删除或“已同意”动作；正式儿童易懂摘要、完整法律文本、处理者联系方式、客服/投诉和真实行权入口仍须由合规工作流定稿发布。

HarmonyOS 静态门把 `main_pages.json` 固定为唯一可达的 `Index` 页面，并要求 `Index.ets` 内只有一个 `@Entry @Component struct Index`；同时约束精确 imports、固定模型导出、Builder/入口/helper/生命周期签名与关键步骤，并拒绝额外事件、网络/存储/路由副作用、动态身份、re-export、decoy Entry/方法、非白名单或额外模板插值和结果分支外业务视图。对抗夹具属于 defense-in-depth，不能替代以后每次涉及儿童数据的信息流人工审查。

验证只使用合成对象、临时工作区和 unsigned 本地构建：功能提交 `08de124` 后 `npm test` 200/200、`npm run check`、`git diff --check`、HarmonyOS 静态安全门 11/11 通过；ArkTS 变更后的临时 unsigned 工程 BUILD SUCCESSFUL、Hypium 43/43；CodeLinter 0 error、0 warning，唯一 suggestion 是现有 1024×1024 启动图标宜导出为不超过 256×256 的获批品牌版本，本轮未替换品牌资源。双重最终复核无 P0/P1。未执行外部联网、HUKS/AssetStore 模拟器或真机验证、HAP/APP 打包、签名、部署或 AppGallery 操作，儿童生产功能门继续关闭。

### S11：微信小程序受控 synthetic 工作区

本切片新增 `npm run prepare:miniapp-synthetic`，只在操作员显式确认获批的 canonical 非生产 HTTPS origin、格式正确且与 canonical 工程不同的 synthetic AppID，以及全新的系统临时输出目录后生成副本。它只把与 `HEAD` 完全一致的 stage-0 Git index 普通 blob 写入临时目录，显式排除项目说明文件，拒绝私有配置、签名/密钥扩展、未知文件类型、符号链接式模式、外部 root 配置、已有输出、UNC、仓库内或非临时路径。未暂存工作树既不检查也不使用。

生成器替换只读运行环境和 `project.config.json`：develop/trial 唯一绑定批准的 synthetic API/法律源并开启监护预览，release/unknown 固定指向不可路由源且零请求；AppID 改为操作员提供的独立 synthetic 字符串，`urlCheck=true`、`uploadWithSourceMap=false`。完整小程序输入树和 app legacy、v2 transport、法律 URL/web-view 链路均受固定审计摘要约束；legacy `fetchAPI` 同时新增严格 `/api/...` 路径规范器，userinfo、子域拼接、绝对 URL、反斜杠、双斜线、fragment 和多层编码穿越均稳定本地拒绝且不调用 `wx.request`。

所有 Git 读取固定 `--no-pager`、`--no-optional-locks`、`core.fsmonitor=false`、`GIT_NO_LAZY_FETCH=1`，清除仓库/配置注入环境，只允许 `rev-parse`、`ls-files --stage`、`ls-tree HEAD` 和 raw `cat-file --batch`；因此不触发工作树 attributes/filter，也不会在 promisor clone 自动拉取缺对象。生成文件集、每个文件、manifest、源/生成树和生成器实现摘要在 staging 内逐字节验证，目录 rename 是成功路径最后一次文件系统操作，避免发布后路径递归回滚竞态。

manifest 准确区分本地证据与外部硬门：只证明 AppID 字符串不同且操作员已确认，不证明 AppID provisioning、开发者权限、request/business domain、DevTools 私有配置或基础设施连通；命令本身不联网、不启动 DevTools、不 preview/upload、不部署。验证只使用合成 origin/AppID 和系统临时目录：`npm test` 212/212，小程序环境/安全门/synthetic 定向 28/28，`npm run check`、`git diff --check` 通过，三路最终只读复核 P0/P1 为 0。S11 只推进成人受控 synthetic 预发布准备，不能供儿童使用或开启生产功能门。

### S12：合成运行配置与已提交预检门

本切片新增显式 `DEPLOYMENT_TIER=synthetic` 运行契约。synthetic 仅在 `NODE_ENV=production` 下生效，要求三项逐字确认、独立 canonical HTTPS API/法律源、与生产不同且格式固定的微信 AppID、显式注入的 AppSecret、合成数据集 ID、低敏日志级别和明确的 direct/trusted-proxy 配对来源策略。四个 synthetic 核心门必须逐项为 `true`，数据行权与两项 legacy 门必须逐项为 `false`；production tier 则继续把全部儿童门锁为 `false`、法律发布配置锁为空，并拒绝任何 synthetic 标记混入。

运行数据只允许位于仓库外、命名受限且预先物理创建的本地绝对根。启动和数据库/Token 首次访问都会复核无符号链接的真实目录链、精确 marker、唯一 `data` 子目录、固定 synthetic SQLite 文件名和仅允许的 SQLite/secret 文件集；SQLite 打开、WAL/migration 前后重复复核边界，Token secret 以 `wx` 和 `0600` 创建且形态固定。synthetic 不读取旧 JSON 用户、不产生自动或 API 备份；备份 API 仍先鉴权，避免把禁用状态变成匿名运行探针。微信认证不再回退读取仓库根 `.env`。合成 E2E 回归安装 loopback-only 守卫，禁止 DNS、TLS、UDP、非 loopback HTTP/TCP、子进程、worker 和 cluster 逃逸。

`npm run verify:synthetic-api-preflight` 使用内部 synthetic fixture 自检提交实现、锁定 Git 身份与 offline guard，生成的临时 artifact 验证后即删除；实际候选 schema 4 artifact 必须由同一候选配置执行 `npm run preflight:synthetic-api -- --output <系统临时目录下全新绝对目录>` 生成。普通 preflight 与 S15/S16 provenance 都复用锁定的绝对 Git 身份，不执行 PATH 前端的临时假 Git。S12 最初锁定 13 个，S13 加入数据根工具，S14 纳入 bootstrap、runtime 回执门、密码边界与 001～010 全部迁移，S15 纳入候选证据 CLI/支持层，S16 再纳入外部签名束 verifier，当前精确锁定 36 个。schema 4 把规范化可信代理集合与 AppSecret 身份纳入不回显明文的配置聚合绑定。迁移执行器另使用显式十项审计清单，并拒绝目录中额外、缺失或非普通 SQL，避免未来或未跟踪迁移绕过来源证明后被执行。外层 verifier 预先解析并锁定绝对 Git 可执行文件，内层 guard 以两轮各 8 步的 `root → HEAD → index → tree → batch → HEAD → index → tree` 状态机锁定 commit、OID 宽度、index/tree 签名、batch 请求和逐帧 blob 字节；证据写入/读回只能在两轮之间发生，第二轮完全一致后原子 rename。guard 同时封锁网络、SQLite、非 Git 子进程、worker、动态 import、模块注册/加载、进程底层入口、随机数等非授权 crypto 和非授权文件 API，只允许预检所需的 hash/HMAC 纯计算，任一违规永久毒化该次运行。测试包含假 `git.exe`、乱序/错误 OID/重排或重复 batch、计算式动态 import、后注册 hook、net server、随机数、文件/进程/primordial 绕过等对抗夹具。

证据只证明：注入配置的形态通过、当前 36 个运行实现与已提交 HEAD 相同（仅允许 LF/CRLF 规范化差异）、十项迁移集合精确、预检命令没有启动服务、打开数据库、联网、调用 DevTools、preview/upload、部署或尝试更改生产儿童门。配置聚合绑定真实代理集合与 AppSecret 当前身份，但不证明其外部独立性或托管合规。它也不证明 `PORT`、真实 AppID provisioning/开发者权限、物理目录 ACL/所有权、数据库内容隔离、正式法律记录、DNS/TLS/基础设施连通或已部署状态；`productionChildGate.deployedStateVerified=false`，`changeAttempted=false` 只描述本次离线命令。S12 没有物理建立外部 synthetic 环境，不能供儿童使用或开启生产功能门。

### S13：合成数据根离线准备与核验

本切片新增 `npm run prepare:synthetic-data-root` 与 `npm run verify:synthetic-data-root`。两个 CLI 除 `--help` 外不接受参数，只读取当前进程显式注入的完整 S12 synthetic 配置；候选根还必须是 `SYNTHETIC_DATA_ROOT_APPROVED_PARENT` 所指现存真实父目录的直接子项。prepare 额外要求逐字确认 `SYNTHETIC_DATA_ROOT_PREPARE_ACK=prepare-new-empty-synthetic-root-v1`，并且只对完全不存在的候选根执行排他创建。

prepare 的成功路径只创建根、唯一空 `data` 子目录和精确 marker，写入后再次核对配置、父目录身份、目录身份、普通单链接 marker 与精确字节；不会打开 SQLite、生成 Token secret、联网、启动服务或子进程。失败或中断后可能保留不完整残根，但工具绝不自动回滚、删除、覆盖或接管，必须按操作手册由受权操作员人工核对和处置。

verify 对候选根执行两轮只读物理快照，复核父目录、根、`data`、marker、允许的固定运行文件、真实路径、设备/inode、链接数、元数据和 marker 摘要；任一快照发现不安全或两轮快照捕获到漂移时都 fail closed。同权限写入者在两次观察之间修改后恢复的瞬态竞态无法由 Node 路径 API 彻底排除，仍依赖外部 ACL/所有权硬门。成功只输出 schema 1 脱敏 readiness：配置和文件系统形态结论；AppID/开发者/AppSecret、request/business domain、DNS/TLS、OS 账号、ACL/owner、磁盘/备份、数据库内容、基础设施、法律记录和生产根隔离均保持 `false`；部署、数据库、网络、服务、子进程、DevTools、preview/upload、成人 smoke 与 HUKS/AssetStore 操作事实也均为 `false`。`productionChildGate` 只说明本命令未验证部署状态、未尝试改门。输出不含明文 origin、AppID、AppSecret、绝对路径、marker 正文或数据库内容。

S13 只提供可重复的离线创建与核验工具，没有实际创建获批外部数据根，也没有验证 AppID provisioning、开发者权限、AppSecret 独立性、request/business domain、DNS/TLS、OS 账号、ACL/所有权、磁盘/备份、数据库内容、法律记录、基础设施、部署状态或成人设备 smoke。测试只使用合成配置和系统临时目录；最终验证数字见 S13 交接。S13 不能供儿童使用，也不能开启 production 儿童功能门。

### S14：synthetic 初始管理员与法律证据安全引导

本切片新增 `npm run bootstrap:synthetic-database` 和迁移 `010_synthetic_bootstrap_receipt.sql`。CLI 不接受参数，只从获批不落盘生产者连接的非 TTY stdin 读取最多 16 KiB 的单行 canonical JSON；管理员密码不得来自环境、普通文件、命令行、日志或证据。输入固定为一个 `synthetic_admin_` 成人管理员、四种各一次的获批 synthetic 法律元数据和合成批准引用，监护关系声明继续来自完整 S12 环境。工具不接受真实身份、正式法律正文、生产资源或业务数据。

bootstrap 只接受此前不存在的固定 SQLite 路径，不接管已迁移空基线、删空旧库、未知 schema、预生成 secret 或任何业务行。主文件以 `wx`/私有模式排他预建，SQLite 打开前后复核普通单链接文件身份；迁移、默认家庭收敛、scrypt 管理员、四类法律元数据和单例回执在同一 `BEGIN IMMEDIATE` 内完成。提交前失败完整回滚并只在能证明是本次文件身份时移除空库；提交结果未知只能用原请求立即恢复，无法证明时必须人工隔离。并发同请求形成一个 `created` 与安全 `replayed`，不同请求只允许一个提交，其余稳定冲突。

回执不可插入第二条、不可更新、不可删除，并绑定 deployment、S13 marker、schema、dataset、批准引用、管理员、管理员 verifier、监护关系声明摘要，以及同一 S12 artifact 的来源 commit、实现树摘要和配置摘要。bootstrap 只校验 provenance 形状并固化，不认证外部批准；S15 才把它与当前 HEAD/config/live receipt 精确绑定。synthetic runtime 在任何可写数据库打开、迁移或 Token secret 创建前只读验证当前 schema、回执和已绑定环境值；缺失返回 `SYNTHETIC_BOOTSTRAP_REQUIRED`，绑定值变化返回 `SYNTHETIC_BOOTSTRAP_CONTEXT_MISMATCH`。S14 fingerprint 不含绝对根、主机或 inode，相同配置/marker 下的物理复制仍须由 S15 root context 与外部 ACL/所有权/生产根隔离共同控制。runtime 允许完成后的管理员密码轮换和微信绑定，但 bootstrap 不是轮换或法律更新接口；一旦运行状态演进，不得再把旧请求当作一般状态查询。旧 S14 迁移 010 数据库不得升级或接管，必须从当前候选的全新 S13 根重新引导。

成功只输出 schema 1 脱敏证据，并区分首次写入数与 replay 的零写入数；所有外部 AppID/权限/AppSecret、法律发布、DNS/TLS、ACL、部署审批和成人设备 smoke 仍保持 `false`。S14 没有创建任何外部根、账号或页面，没有启动服务、联网、部署或开放 production 儿童门；完整操作与残留处置见 `docs/runbooks/受控-synthetic-初始管理员与法律证据引导.md`。

### S15：候选机器证据离线绑定与未认证声明信封

本切片新增 `npm run capture:synthetic-candidate-evidence` 与 `npm run finalize:synthetic-candidate-evidence`。两者只从非 TTY stdin 接受最多 256 KiB 的单行 canonical JSON，并要求 `SYNTHETIC_CANDIDATE_EVIDENCE_ACK=assemble-review-only-not-deployment-v1`。Phase A 精确核对 S12 schema 4、S13 bootstrap 前 evidence、S14 evidence、当前已提交 36 文件/10 迁移、当前 S12 配置聚合与 live receipt；它以只读 immutable SQLite 核对一个家庭、一个未轮换成人管理员、四类法律元数据、一个回执和零儿童业务状态，并用前后数据库字节/元数据快照、物理根/父目录/文件身份及主机/OS 账号摘要约束当前观察。

Phase A 只生成 30 分钟 machine subject。`subjectSha256` 与所有绑定摘要都未签名；`historicalSequenceVerified=false`、`runtimeSecretIndependenceVerified=false`、`localClockExternallyTrusted=false`。当前 pristine 结论不证明历史顺序，且同权限写入者在观察间修改再完整恢复的瞬态风险仍依赖外部 ACL/所有权和审计。

Phase B 重新执行机器状态核验，要求 19 项固定顺序声明分别绑定 subject、角色、来源、证据引用摘要与时间。它只接受 `state=declared_satisfied_not_authenticated` 和 `signatureStatus=not_verified`，并明确输出 `attestationAuthenticityVerified=false`、`externalFactsVerified=false`。两命令都不持久化证据、不联网、不写 synthetic SQLite、不部署、不读取生产门；`externalFactsVerifiedByThisCommand=false`、`deploymentAuthorization=not_granted`、`productionChildGateState=not_observed`、`childUseAuthorization=not_granted` 始终保持。完整契约见 `docs/runbooks/受控-synthetic-候选机器证据与未认证声明信封.md`。

### S16：外部签名束离线验证与未消费授权

本切片新增 `npm run verify:synthetic-external-approval`。CLI 要求 `SYNTHETIC_EXTERNAL_APPROVAL_ACK=verify-signatures-only-not-deployment-v1`，只从非 TTY stdin 接受最多 512 KiB 的单行 canonical JSON；输入不能携带公钥或策略正文。公开 Ed25519 策略必须位于仓库和 synthetic 数据根之外、与 canonical 仓库同一受控本地卷的独立普通文件，以环境 SHA-256 钉住；路径必须 canonical、无 UNC/device/异卷/任一祖先 junction 或 symlink，工具在验证前后两次核对文件身份和内容。策略只覆盖 `external_gate_verifier`、`deployment_approver`、`deployment_grant_issuer` 与 `revocation_authority` 四个职责隔离角色，私钥不进入仓库或命令。

输入包含独立签名的 revocation checkpoint、19 项固定顺序 gate verification、独立 approval 和最长 5 分钟的 `deploy_synthetic_once` grant。所有信封固定 Ed25519 和互不混用的 domain，精确绑定 policy、S15、subject、candidate、machine state、attestation/gate set、source/config/root 派生目标、证据/权威/审计摘要与 principal。checkpoint 不得来自未来且必须不早于全部已签工件，可按 key/principal/approval/grant 撤销；gate declarant/verifier、approval、grant issuer 与 revocation authority 强制职责隔离。审批和 grant 只能保持 production child gate 与 child use 为 `not_granted`。

每次验证先按原 `finalizedAt` 精确重构 S15，再以当前时间重算 committed provenance、配置、物理根和只读 pristine SQLite；策略随后二读，最终有效期取 S15、策略、checkpoint、全部实际使用 key、gate、approval 与 grant 的最早失效时刻，并在命令完成时再次检查。成功只表示 `signed-bundle-valid-against-provided-policy-unconsumed`：本命令不认证策略发布权威，不取回身份、证据正文或审计记录，不信任本机时间，不验证 checkpoint sequence 单调，不持久化 replay/消费状态，不联网、不写数据库、不部署，始终输出 `deploymentAuthorization=not_granted`、`productionChildGateState=not_observed`、`childUseAuthorization=not_granted`。完整契约见 `docs/runbooks/受控-synthetic-外部签名束离线验证与未消费授权.md`。

## 9. 功能门与启用规则

默认值全部为 `false`：

- `harmony_child_enabled`
- `child_enrollment_enabled`
- `device_pairing_enabled`
- `point_requests_enabled`
- `child_data_rights_enabled`

当前服务端环境变量与逻辑门对应如下；只有 `1`、`true`、`yes`、`on`（不区分大小写）会开启，未设置或其他值均按关闭处理：

| 环境变量 | 用途 |
| --- | --- |
| `HARMONY_CHILD_ENABLED` | HarmonyOS/v2 儿童新流程总门；不控制共享旧家长端 |
| `CHILD_ENROLLMENT_ENABLED` | 监护授权与儿童建档 |
| `DEVICE_PAIRING_ENABLED` | 设备配对与设备会话 |
| `POINT_REQUESTS_ENABLED` | 积分申报审批 |
| `CHILD_DATA_RIGHTS_ENABLED` | 儿童数据行权流程 |
| `LEGACY_CHILD_LOGIN_ENABLED` | 仅用于显式兼容验证的旧儿童密码登录，生产保持关闭 |
| `LEGACY_CHILD_MANAGEMENT_ENABLED` | 仅用于显式兼容验证的旧儿童增删改，生产保持关闭 |

新流程开关不能连带开启任何 `LEGACY_*` 开关。

`CHILD_DATA_RIGHTS_ENABLED` 只门控新的 access/export/correct 请求；既有请求状态、回执和已授权导出保持可读，withdraw/delete/terminate 不受该门或 Harmony 总门封锁。删除/终止仍受动作专用重新认证、本人历史监护证据、revision、幂等和策略阻断作业约束，功能门关闭不等于放宽授权。

生产开启设备配对时必须同时明确客户端地址边界：直连部署使用 `PAIRING_CLIENT_IP_MODE=direct`；反向代理部署使用 `PAIRING_CLIENT_IP_MODE=trusted_proxy`，并在 `TRUSTED_PROXIES` 中配置实际可信代理 IP/CIDR。未声明时生产启动拒绝开启设备配对；边缘层仍需独立限流和容量保护。

法律文本服务必须显式配置不带路径、端口、用户信息、查询或片段的精确 HTTPS `LEGAL_PUBLIC_ORIGIN`，并让数据库公开 URL 和监护关系声明公开 URL 都匹配固定类型、版本和摘要叶子。配置缺失或任一证据不匹配时不发布当前法律文本，也不得开启授权建档。

用于设备凭据派生、本人流水及积分申报列表游标 AEAD 的服务端根密钥必须纳入生产密钥托管、轮换与单密钥签发量监测；上线前须明确轮换后旧凭据、旧游标的失效和客户端恢复策略。

共享旧家长端不读取 `HARMONY_CHILD_ENABLED`；它以 `child_privacy_states` 为全渠道合规真值门，并额外要求当前成人本人持有该儿童的 active 监护授权。这样关闭 HarmonyOS 新流程不会误伤已授权家长操作，而 suspended、processing_blocked 等状态仍会阻断所有客户端新增处理。

功能门是应急止损手段，不是绕过正式上架的私有分发方式。首次正式上架前，只有在总体计划的合规、迁移、隔离、删除、备份和发布硬门全部满足后，才可面向符合条件的普通家庭开启核心能力。

## 10. 测试与迁移门禁

新增测试文件建议：

- `server/test/v2-security-regression.test.js`
- `server/test/guardian-consents.test.js`
- `server/test/device-pairing-sessions.test.js`
- `server/test/point-requests.test.js`
- `server/test/data-rights.test.js`
- `server/test/stage1-migrations.test.js`
- `server/test/pre-migration-backup.test.js`
- `server/test/v2-http-errors.test.js`
- `server/test/miniapp-environment.test.js`
- `server/test/miniapp-hardening.test.js`
- `server/test/miniapp-synthetic-workspace.test.js`
- `server/test/synthetic-server-config.test.js`
- `server/test/synthetic-external-approval.test.js`
- `server/test/loopback-only-network.test.js`
- `server/test/harmonyos-security.test.js`
- `hefei-harmonyos/entry/src/test/Client.test.ets`
- `hefei-harmonyos/entry/src/test/Protocol.test.ets`
- `hefei-harmonyos/entry/src/test/Recovery.test.ets`
- `hefei-harmonyos/entry/src/test/PrivacySafetyShell.test.ets`

每个切片至少执行：

```bash
npm test
npm run check
git diff --check
```

迁移测试只使用临时数据库，覆盖 `005 → 006～009`、代码回退、再次前滚、`integrity_check`、备份哈希、家庭数/余额/流水核对。不得以真实数据库作为开发验证样本。

## 11. 当前明确不做

- 不恢复 AppGallery 未成年人邀请测试路线；
- 不向未成年人设备侧载测试包，不要求切换成人账号、关闭未成年人模式或开启开发者模式；
- 不读取或迁移真实儿童数据；
- 不实现照片、Push、广告、支付、第三方统计、用机兑换或时间兑换；
- 不部署、不开放生产功能、不 push；
- 不改父仓、专用远端或任何远端 refs。

## 12. 下一项可直接实施的任务

下一项是 S17“权威外部信任接入、单调吊销与原子授权消费”，不是直接部署。S16 只验证调用方提供并由环境摘要钉住的公开策略与签名束；它不认证策略发布权威，不取回身份、证据或审计正文，不提供可信时间，也不证明 checkpoint 最新或 grant 未被其他消费者使用。后续必须由受权外部系统建立这些事实，并继续隔离任何既有库、残根、未知锁或跨环境复制数据库。

同一候选还必须在权威系统中逐项验证独立 AppID provisioning、开发者权限、AppSecret 托管/轮换、request/business domain、DNS/TLS、基础设施和 synthetic 法律页面版本/摘要/重定向/CSP，并确认 DevTools 私有设置没有关闭域名或 TLS 校验。候选 commit 先运行 `npm run verify:synthetic-api-preflight` 自检，再由同一配置运行 `npm run preflight:synthetic-api -- --output <系统临时目录下全新绝对目录>`，保存不含秘密且锁定 36 个实现文件和精确十项迁移集合的 schema 4 artifact。外部系统还必须持久保证策略权威、可信时间、checkpoint sequence 单调和 grant compare-and-consume 与最终撤销判定的原子性。只有这些硬门全部关闭，才可另行批准一次受控 synthetic 部署；S15 声明和 S16 `unconsumed` 输出都不是部署许可。

获批部署后，HarmonyOS 只使用 S9 临时 profile，小程序只使用 S11 临时工程，不得修改跟踪零联网默认值、允许访问生产 host 或使用运行时改源。先用模拟器或成人受控设备执行完整 smoke，再决定是否推进后续发布候选；不得在儿童设备侧载或绕过未成年人模式。

受控 synthetic API 建成后，在微信开发者工具、HarmonyOS 模拟器或成人受控 API 20+ 设备完成“授权 → 配对 → 查积分/规则 → 申报 → 家长审批 → 查看结果 → Refresh → 撤销”的端到端 smoke，并验证 HUKS、AssetStore、前后台、重启和清数据行为，保存不含个人信息和凭据的结果；发布候选继续重跑 ArkTS 编译、Hypium、静态门和 CodeLinter。

并行以 S10 工程安全壳为边界，由合规工作流补齐儿童易懂版正式摘要、完整法律文本、处理者联系方式、客服/投诉与真实行权入口，同时完成安全文件导出交付/披露回执，以及未决申报文字的保留期限和监护人授权安全放弃策略。申请补充、取消与重新提交必须先有设备作用域单条详情和操作对账契约，不得直接复用成人接口。全过程继续只使用合成家庭；正式法律文本、PIPIA、存量数据整改、逐类留存/删除策略、受托方约束、密钥/加密备份演练、备案和发布审查未完成前不得开启儿童生产功能门。
