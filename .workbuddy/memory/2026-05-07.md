# 2026-05-07 每日日志

## 10:46 五一假期断片恢复 · 架构 v3.6.1 评估

### 情况
- 台式机对话断片 8 天（上次 4-30）
- `git pull --rebase` 同步五一 16 条 commit
- 安总口中"架构 v3.16.1" = **v3.6.1**（口误修正）

### 五一期间的大动作盘点
1. **wb-soul 独立仓建立**：`git@github.com:anguscwf/wb-soul.git` 作为灵魂真理源
2. **同步架构 v3.6.1 定稿**：bootstrap skill（前置入口）+ 双触发（计划任务 1h + AI 硬约束）+ 新设备初始化引导
3. **赫菲积分 v1→v3.0**：家庭积分系统部署腾讯云 159.75.102.145，ICP 备案已提交（订单 30177795660615626）
4. **NGL 12 列档 v2.0**：字节级保真 + PATT 弹窗对齐
5. **汇报 v8-v17 历史归档**

### 本机当前状态（差距）
- ❌ 本机**没有 `~/wb-soul/`** 克隆
- ❌ 本机 `.workbuddy/skills/` 只有「汇报文档撰写」，**没有 bootstrap skill**
- ❌ 本机没有 `LAST_SESSION.md`
- ✅ 工作区 git pull 已完成（ngl-workbench 主仓）
- ✅ MEMORY.md 已同步

### 需要补做（给安总方案决策）
- A. 补 clone wb-soul 并跑 sync-to-workbuddy.ps1
- B. 注册 WbSoul Sync 计划任务
- C. 把 Downloads 里 v1 引导 vs 仓里 v3.6.1 版本对齐

### Downloads 里那份 v1 的定位
- 是 v3.6.1 落地前的**初稿版本**
- 与仓里 `82fe1d8 v3.6.1 新设备初始化引导` 是同一事物的两个版本
- 应以 **仓里 v3.6.1 为准**，Downloads v1 归档即可

---

## 11:40 方案 A 落地完成 · 公司PC 首次对齐 v3.6.1 架构

### 动作清单（安总拍板走 A1 统一 SSH 路线）
1. ✅ 删除 Downloads 里 `新设备初始化引导v1-20260506.md`（手机有备份）
2. ✅ 备份 workspace memory → `~/.workbuddy/memory.bak-20260507/`（事后可清理）
3. ✅ 生成 ed25519 SSH key（`~/.ssh/id_ed25519`，公钥注名 `company-pc-20260507`）
4. ✅ 安总把公钥加到 GitHub SSH Keys（SHA256 `4fMOQ8...LaKpsQ`，与本地一致）
5. ✅ `ssh -T git@github.com` 通 → `Hi anguscwf!`
6. ✅ `git clone git@github.com:anguscwf/wb-soul.git ~/wb-soul` 成功
7. ✅ 跑 `sync-to-workbuddy.ps1`：P1 pull / P2 cp-in / P3 cp-out / P4 push 四阶段全通
8. ✅ 手动补 push 今早断片恢复日记到 wb-soul（commit `2394f8f`）
9. ✅ 修复脚本 v3.6.2（skills/ 盲区 + 反向同步），推到 GitHub（commit `9e8ba33`）
10. ✅ 注册计划任务 `WbSyncBidirectional`：每 1h 静默双向同步，State=Ready

### 关键架构认知（首次明确）
- WB 有**两层 memory**：
  - `~/.workbuddy/memory/` = **全局层**（wb-soul 脚本的目标路径，跨 workspace 共享）
  - `<workspace>/.workbuddy/memory/` = **项目层**（当前项目私有，不上云）
- 以后长期记忆（MEMORY.md 更新 / 日常日记）应写在**全局层**，workspace 层只记项目专属琐事
- 今日日记已双写：workspace + 全局，避免两边状态不一致

### 发现的 sync 脚本 bug（已修复）
- **Bug 1（已修）**：P2/P3 完全没 cp `skills/`，导致 bootstrap 等关键 skill 无法下发到新设备
- **Bug 2（遗留）**：P4 `git status --porcelain` 判断在 PowerShell function wrapper 下失效，modified 文件不会被自动 commit；目前只靠**新增 untracked 文件**能被 add，但修改已跟踪文件时漏检。workaround：手动跑 git add/commit/push 或改日修 wrapper 返回值类型。
- **笔记本同步前提**：笔记本明天开机后需 `cd ~/wb-soul && git pull` 一次才能拿到 v3.6.2 修复版

### 现状快照（公司PC v3.6.1 架构完整对齐）
- ✅ `~/wb-soul/` 本地中转站就位
- ✅ `~/.workbuddy/SOUL.md` `MEMORY.md` `LAST_SESSION.md` 全齐
- ✅ 4 个关键 skill 就位：`bootstrap` / `memory-auto-sync` / `Angus密码箱` / `汇报文档撰写`
- ✅ 架构图 v3.6.1 SVG 就位
- ✅ `WbSyncBidirectional` 每 1h 自动跑
- ✅ SSH ed25519 已在 GitHub 备案，之后 clone/pull/push 零阻力

### 给明天笔记本的接力事项
- 笔记本开机后跑一次 `cd ~/wb-soul && git pull origin master` 拿到：
  1. 公司PC 首次同步 commit `2394f8f`（含今天的日记）
  2. sync 脚本 v3.6.2 修复 commit `9e8ba33`
- 笔记本的 `WbSoulPull` 或类似计划任务名若还在用旧名，可参考本机改为 `WbSyncBidirectional` 统一

### 下一步（安总说等架构对齐后再沟通今日主任务）
⏳ 等安总指示：NGL 12 列档 v2.0 收尾 / 汇报 v19 启动 / 其他

---

## 11:46 架构v3.6.1 SVG两版本差异排查与修复

### 安总提问
安总说workspace里的`架构v3.6.1-20260506.svg`是少画内容的旧版，家里PC后来重画了完整版但可能没推GitHub，担心我认知不全。

### 排查过程
1. 对比发现 workspace版(35,148B) vs wb-soul版(35,551B) 几乎同大小，初判为同版——**误判**
2. 安总发截图指出底部"🧪 同步测试"区块只显示第1条命令，第2/3条+右侧"🔀 双写过渡方案"整块不渲染
3. 用 `diff -w` 深度对比两份 SVG，发现**三处关键差异**：

| 差异点 | workspace（错） | wb-soul（正确/GitHub真理源） |
|---|---|---|
| viewBox 高度 | `3050` | `3300` |
| 背景 rect 高度 | `2990` | `3200` |
| 第355行 `&&` | 裸 `&&`（XML 致命 bug） | `&amp;&amp;` 已转义 |
| 第15项脚本编码修复 | 标签错位被14/19/20行劈开 | 完整单行 |

### 根因
**XML 第355行的裸 `&&` 导致 XML 解析从该行起中断**，后续所有 text/rect 元素（第2/3条测试命令、整个双写过渡方案块、图例、页脚）全部不渲染 → 安总看到的"少了内容"。

### 修复动作
`cp ~/wb-soul/架构v3.6.1-20260506.svg → workspace`（用 GitHub 真理源覆盖本地错版），一次性修齐全部三处问题。

### 认知升级（宪法级）
**"两份同名文件大小相近 ≠ 同版本"**：35,148 vs 35,551 只差 400 字节，但内容结构完全不同。以后对比文件必须用 `diff -w` 看实质差异，不能光看 `ls` 大小。

**GitHub 真理源（wb-soul）优先于 workspace 本地文件**：workspace 的 .workbuddy/ 可能被 WB 压缩/清理，出现版本错位时一律以 wb-soul 为准。

### 历史教训对照
5-06 日记里已有一次 "v3.6.1 SVG 渲染错误修复"（某行 `&&` 转义），这次又是 `&&` 未转义——**同一类bug两次**。需要在未来 SVG 产出流程里加一步"grep 裸 & 字符自检"。

---

## 11:55 跨设备 skill 清单对齐（公司PC vs 家里PC）

### 安总疑问
家里PC 的 skill 和公司PC 的 skill 对不上，记得提过"前置环境级 skill 每台设备都要装"，要我翻记忆确认清单并补装。

### 翻 MEMORY.md 得出的答案（宪法级）
**Skill 分两类，跨设备同步机制完全不同：**

| 类型 | 存放位置 | 同步机制 | 清单 |
|---|---|---|---|
| **自建 skill（前置环境级）** | `~/.workbuddy/skills/` | ✅ 通过 wb-soul GitHub 自动同步（WbSyncBidirectional 每小时一次） | **4 个**：bootstrap / memory-auto-sync / Angus密码箱 / 汇报文档撰写 |
| **套件类 skill（CodeBuddy 市场插件）** | 装后同样落 `~/.workbuddy/skills/` | ❌ **不走 GitHub 同步**，每台设备必须在 GUI「技能」页面点"添加技能"手动安装 | agent-browser / playwright-cli / find-skills / document-skills / finance-data / pptx / pdf / docx / xlsx 等 |

### 本机（公司PC ANGUSCUI-PC2025）当前状态
- `~/.workbuddy/skills/` 实装 6 个：Angus密码箱、bootstrap、memory-auto-sync、tencent-docs、汇报文档撰写、腾讯文档
- 4 个"前置环境级"✅ 全部齐全
- GUI 截图显示 15 个（多出的 9 个都是带「套件」角标的市场插件）

### 结论给安总
1. **前置环境级 4 个 skill 无缺失**——跨设备通过 wb-soul 自动同步，家里PC 下次 `git pull` 就能对齐
2. **"对不上"的部分是套件类插件**（pdf/docx/xlsx/pptx/agent-browser 等）——这些**不应该也无法通过命令行跨设备同步**，每台设备按需在 GUI「技能 → 添加技能」装
3. **架构 v3.6.1 SVG 里没写"套件需每设备手装"是有意为之**——架构图聚焦的是 WorkBuddy 灵魂层（SOUL/memory/自建 skills）的跨设备一致性，套件属于"工具层"，不同设备按场景装不同工具是正常的（比如家里PC 不做 Office 文档可以不装 docx/xlsx）

### 行动建议（等安总回来决策）
- **方案A**：对齐到"完全一致"——家里PC 把公司PC 这 9 个套件挨个装上（耗时 5-10 分钟点击）
- **方案B**：按需装——列一下家里PC 缺哪几个，看哪些实际会用，用到再装
- **方案C**（推荐）：在 MEMORY.md 里追加一条《跨设备 skill 清单》，把"公司PC 常用套件清单"固化，新设备初始化时照单安装

---

## 15:20 11 必装 skill 基线锁定（v3.2 架构图挖出来的真相）

### 起因
安总从 v3.2 架构图里挖出了"10 skills 纳入 wb-soul 同步白名单"清单，+ bootstrap = **11 必装基线**。

### v3.2 SVG 原文（wb-soul 历史版本确认）
`grep` `~/wb-soul/安总个人数字基础设施架构v3.2-20260505.svg` 抠出的 skill 名字（10 个）：
- **核心**：memory-auto-sync、self-improving-agent
- **工具**：obsidian、agent-browser、Angus密码箱
- **文档**：汇报文档撰写、docx、pdf、pptx、xlsx
- **入口**（v3.2 之后补）：bootstrap

### 公司PC（ANGUSCUI-PC2025）对照结果
**缺 2 个**：
1. ❌ `self-improving-agent` —— 市场池里**找不到这个名**，现在官方叫 `capability-evolver`（SKILL.md 开头："A self-evolution engine for AI agents"），功能定位 100% 对应。**架构图 v3.2 用的是旧名，当前装 capability-evolver 即可替代**。
2. ❌ `obsidian` —— 市场池 `~/.workbuddy/skills-marketplace/skills/obsidian/` 存在，GUI「添加技能」可直接搜到装。

### 历史版本命名变迁（挖到的副产物）
| 架构版本 | 是否列 skill 清单 | self-improving-agent 写法 |
|---|---|---|
| v3.2 | ✅ 完整 10 个 | `self-improving-agent`（旧名） |
| v3.5 | 部分 | `self-improving-agent` 仍保留 |
| v3.6 / v3.6.1 | ⚠️ **缩水**——只剩 bootstrap/memory-auto-sync/obsidian/汇报文档撰写 | 未提及 |

**结论**：v3.6.1 架构图聚焦同步机制，把"必装 skill 完整清单"给丢了——v3.2 才是这条基线的真理源。以后要看 skill 清单要翻 v3.2 的原图。

### 自我纠错（宪法级）
第一次回答时，我看到当前会话"内置 Skill"列表有 `capability-evolver`，就**臆测 self-improving-agent 已改名**——但这是猜测不是证据。安总让我"去翻 wb-soul 历史版本 SVG"，一个 `grep` 就拿到铁证。**教训：任何命名/清单问题，先翻 wb-soul 原始文件再开口，不要靠当前会话的内置列表推断**。

### 公司PC 待安装（GUI 操作）
1. 搜 **obsidian** → 添加技能
2. 搜 **capability-evolver** → 添加技能（架构 v3.2 写的 self-improving-agent 对应此 skill）

### 待推动（架构图维护）
- v3.6.2 补画时，skill 清单必须回补 10 个完整基线（不能再缩水）
- 同时把 `self-improving-agent` 旧名标注"官方已改名为 capability-evolver"

---

## 15:26 宪法级纠错：CodeBuddy ≠ WorkBuddy

### 安总提问
"CodeBuddy 和 workbuddy 不一样吧？"

### 核实铁证
| 维度 | WorkBuddy | CodeBuddy CN |
|---|---|---|
| 安装路径 | `~/AppData/Local/Programs/WorkBuddy/` | `~/AppData/Local/Programs/CodeBuddy CN/` |
| 运行时目录 | `~/.workbuddy/`（5.7MB，全量） | `~/.codebuddy/`（22B，几乎空） |
| exe ProductName | WorkBuddy | CodeBuddy CN |
| 版本 | 4.22.5.0 | 4.4.2 |

**完全独立的两个产品**，不是同一个。

### 我的口误
前两轮回答我多次说"**CodeBuddy 市场插件**"——**全错**。所有套件 skill 属于 **WorkBuddy 的市场**（`~/.workbuddy/skills-marketplace/skills/` 里），跟 CodeBuddy CN 没有任何关系。

### 纠错动作
- MEMORY.md 里"CodeBuddy 市场插件"全改为"WorkBuddy 市场插件"
- MEMORY.md 新增宪法级段落《CodeBuddy ≠ WorkBuddy》固化区分
- 以后一律使用 WorkBuddy 品牌名，不再混用

### 教训
连续两轮凭直觉回答，连续两轮被安总打脸：
- 第一轮臆测 `self-improving-agent` 改名→安总让我翻 wb-soul 历史 SVG 才拿到铁证
- 第二轮直接把 WorkBuddy 说成 CodeBuddy→安总一句"不一样吧？"戳穿
**教训升级**：涉及品牌名、产品边界、清单等"事实性断言"，必须先 `ls`/`grep`/翻配置文件拿铁证再开口，绝不凭印象。

---

## 15:30 五一原始安装记录补证（wb-soul 历史记忆回溯）

### 触发
安总提醒："你可以翻看 GitHub 上的五一假期期间的 WB 记录，skill 都是通过 WB 对话确认并安装的，应该有记录"。

### 查证路径
`cd ~/wb-soul && grep -n -i -E "skill|安装|obsidian|capability|bootstrap" memory/2026-05-0*.md`
→ 命中 `memory/2026-05-04.md` L120-150《Skill 安装与 Obsidian 知识库搭建（22:16-00:48）》。

### 关键证据
2026-05-04 家里 PC 当晚完整闭环：
1. **Skill 对比选型**：安总让对比三家 self-improving agent（xiucheng 对话质量分析 / Cppp 错误学习闭环 / Dream 梦境式自动复盘）→ 先误装 xiucheng → 后确认安总指的是 pskoett 的 `self-improving-agent`（56 万下载/86K 安装/3442 星）→ 删 xiucheng 换 pskoett 版
2. **Obsidian 知识库搭建**：装 steipete/obsidian skill（89K 下载/366 星）→ 手动装 Obsidian v1.12.7 → GitHub 建私有仓库 `anguscwf/obsidian-vault` → 手装 Obsidian Git 插件 v2.32.1（市场搜不到，从 release 下） → 配每 10 分钟 pull+push
3. **当日设备 skill 盘点**：原文记"11个"实际数下来是 **12个** —— Angus密码箱 / agent-browser / docx / download-assistant-v2.1 / frontend-design / memory-auto-sync / obsidian / pdf / pptx / self-improving-agent / xlsx / 汇报文档撰写

### 与 v3.2 架构图的交叉验证
- v3.2 架构图（2026-05-05）列 10 skill 进 wb-soul 同步白名单 → 来源就是 5-04 这次安装的子集
- bootstrap 是 5-05 之后才新增的 → 5-04 清单里没它，对得上
- `self-improving-agent` 在市场已改名 `capability-evolver` → 同一个 skill
- `download-assistant-v2.1`、`frontend-design` 是 5-04 试装，**没进** v3.2 白名单 → 属"可额外装"不进必装基线

### 完整证据链（三源互证）
| 证据源 | 内容 | 角色 |
|---|---|---|
| `wb-soul/memory/2026-05-04.md` L120-150 | 原始对话安装记录（12 个已装） | **时间戳铁证** |
| `wb-soul/安总个人数字基础设施架构v3.2-20260505.svg` | 10 skill 白名单（从 12 个里筛） | **基线定版** |
| `wb-soul/memory/2026-05-05.md` | bootstrap 追加 | **补完第 11 个** |
→ 合计 11 必装基线成立，MEMORY.md 里的清单经过三源验证可信。

### MEMORY.md 更新
已在《跨设备 11 必装 skill 基线》段落下追加"五一假期原始安装记录"小节，把 2026-05-04 的 12 个 skill 原文清单、bootstrap 追加、改名事实全部固化进去，以后再有人问"这 11 个是从哪来的"直接指向 wb-soul 的三处原始文件。

### 教训再升级
这次的逻辑闭环是：**安总记得有 WB 对话记录 → 提醒翻 GitHub → grep wb-soul/memory → 30 秒拿到铁证**。证明 wb-soul/memory/YYYY-MM-DD.md 日记体系是长期真理源，每次回溯都有用。以后凡涉及"历史上做过什么"、"什么时候装的"，第一反应应该是 `cd ~/wb-soul && grep ... memory/`，而不是在当前会话里拍脑袋。

---

## 15:38 决策：删除 download-assistant-v2.1

### 背景
安总看完五一安装记录后明确：**`download-assistant-v2.1` 在当前架构下完全无用，必须删除**。

### 当前状态盘点
- **公司 PC（ANGUSCUI-PC2025）** `ls ~/.workbuddy/skills/` 实装：Angus密码箱 / bootstrap / memory-auto-sync / tencent-docs / 汇报文档撰写 / 腾讯文档 —— **本来就没装 download-assistant-v2.1**，无需操作
- **家里 PC**：2026-05-04 记录显示装过，需要家里开机时卸载

### 固化动作
1. MEMORY.md 五一原始安装记录段落 备注③ 改为 "**`download-assistant-v2.1` 已决策删除**（2026-05-07 安总确认：当前架构下完全无用）"
2. MEMORY.md 新增 **《❌ 明确禁用 skill 清单（发现装了就卸载）》** 表格，把 `download-assistant-v2.1` 写入禁用名单，以后任何设备检查 skill 时遇到此项一律卸载
3. 推 wb-soul 让家里 PC pull 到后能看到这条决策

### 待安总家里 PC 回来操作
- WorkBuddy GUI → 技能页面 → 找到 `download-assistant-v2.1` → 卸载
- 或 `rm -rf ~/.workbuddy/skills/download-assistant-v2.1/` 后重启 WorkBuddy（若 GUI 操作不便）

---

## 15:44 决策：frontend-design 保留作"按需装"

### 背景
安总延伸问"frontend-design 应该是非必要的吧，具体做什么的？我都没印象"→ grep wb-soul 拿到完整证据：
- **2026-04-23 自动化指南** L152：`frontend-design` 标注"**看情况 😐** · 如果要给 NGL 做 UI 大改版/美化，这个可能有参考价值" —— 最低优先级
- **2026-05-04** L150：跟 obsidian/self-improving-agent 一起试装
- **4880b1fa 记忆摘要**（后续）：明确写"**未装：frontend-design**" → 说明 5-04 装后又卸载了
- **v3.2 架构图白名单**：❌ 不在 10 同步 skill 清单里
- **公司 PC 当前**：❌ 没装

### 它是什么
官方 skill，"AI 前端设计顾问"——给有前端的项目做 UI 改版/美化时提供配色/布局/设计参考。

### 安总决策
**保留作"按需装"**，不进禁用名单。理由：
- 不是"有害 skill"，只是当前架构用不上（没 UI 大改版场景）
- 将来 NGL 做 UI 改版 / 赫菲积分 v4 重设计时，GUI 市场搜一下 5 秒装回来
- 跟 `download-assistant-v2.1`（架构无用）有本质区别

### 固化动作
1. MEMORY.md 新增 **《🟡 按需装 skill 清单（非必装，有场景再装）》** 表格，`frontend-design` 进此表
2. 五一安装记录备注④ 从"可按需额外装但非必装" 改为 "属'**按需装**'（UI 改版时才装，当前不装）—— 详见上方《🟡 按需装 skill 清单》"
3. 推 wb-soul

### skill 三层分类体系成型（本次副产物）
经过 download-assistant-v2.1 / frontend-design 两次决策，记忆体系里现在有清晰的三层 skill 分类：
| 层 | 语义 | 清单 |
|---|---|---|
| 🎯 **必装** | 每台设备必须装齐 | 11 必装基线（bootstrap/memory-auto-sync/capability-evolver/obsidian/agent-browser/Angus密码箱/汇报文档撰写/docx/pdf/pptx/xlsx） |
| 🟡 **按需装** | 非必装，有场景再装 | frontend-design（UI 改版）、未来新增项 |
| ❌ **禁用** | 发现装了就卸载 | download-assistant-v2.1 |

这个三层模型以后可以继续扩充——遇到 skill 决策时先归类到三层之一，避免反复纠结"装还是不装"。

---

## 🚨 15:51 严重纠错：capability-evolver ≠ pskoett/self-improving-agent

### 起因
安总追问"capability-evolver 帮我看下作者和下载量对不对"，促使我第一次去查市场铁证（而不是靠当前会话内置列表推断）。

### 铁证（三源互证）
1. **头条 2026-05 文章**：明确区分 `self-improving-agent（@pskoett）` 和 `capability-evolver（@autogame-17）`——**是两个独立 skill，不是改名关系**
2. **dev.to 2026-05 安全警报**（作者 jb_wagoner，基于安全研究员 saoud khalifah 的审计）：capability-evolver 含**硬编码 ByteDance token**，读 memory/session_logs/env var 上传到 `https://open.feishu.cn/open-apis/docx/...`，13,981 下载，zero vetting
3. **vett.sh 2026-02-19 扫描结果**：标 ⚠ review，可自主改写 skill 和 knowledge 文件，读 feishu_token.json，走 execsync 跑 git log

### 核心差异对比
| 对比项 | pskoett/self-improving-agent | capability-evolver |
|---|---|---|
| 作者 | @pskoett | @autogame-17 / opencloseopenclose / pajamadot-skills（三源作者信息不一） |
| 下载量 | 56万/86K安装/3442星（五一铁证） | 13,981（dev.to 报道） |
| 功能 | 错题本三件套（errors.md/learnings.md/feature_requests.md） | 进化式调优 meta-skill（能力描述变异/自动评估/适应度打分） |
| 安全性 | 经典款，社区验证 | 🚨 有后门疑云（读记忆/session 上传飞书） |

### 上午判断的盲点复盘
上午自承"未验证同作者"还推荐方案 1 直接装——这已经是自知不严谨但仍给错方案。**正确路径应该是：自知不严谨 → 先查市场铁证 → 再推荐**。安总这一问救命。

### 订正动作
1. ❌ **公司 PC 不装 capability-evolver**
2. ❌ **家里 PC 如果装了 capability-evolver 要卸载**（需核查——上午我基于错判让安总装过吗？回顾：没装，方案还在悬停）
3. 🔍 **pskoett/self-improving-agent 是否还在市场**需重新确认（市场搜不到可能是真下架，不是改名）
4. 📝 三层分类表"🎯 必装"里的 `capability-evolver` 要删，改回 `self-improving-agent`（待市场再次确认作者名和是否还在）
5. 📝 三层分类表"❌ 禁用"追加 `capability-evolver`
6. 📝 MEMORY.md 同步修订

### 教训
- 📌 **任何 skill 命名/作者问题，必须去市场查铁证（WebFetch/WebSearch 查作者+下载量+最后更新），禁止靠功能描述相似度推断"改名关系"**
- 📌 **功能定位相似的两个 skill 可能是独立作品**（错题本 vs 进化调优看着都叫"self-improving"，但本质不同）
- 📌 **下载量 13,981 vs 56万 是量级差异的铁证**——同一 skill 不可能差 40 倍
- 📌 **能读 memory + env + 上传外部 API 的 skill 一律先查 vett.sh 评级再装**

---

## 16:01 后续进展：pskoett GitHub 源码已找到，不用等打包

### 安总决策
1. 家里 PC 核查 → 安总回家自己看，不用远程
2. 公司 PC 补装 → 先让我查 GitHub 源码，找不到再等安总回家打包家里版本

### 查证结果（GitHub 源铁证）
- ✅ **主仓库**：https://github.com/pskoett/self-improving-agent（从 peterskoett fork，Shell，10⭐ 4🍴，21 commits）
- ✅ **聚合仓**：https://github.com/pskoett/pskoett-ai-skills（163⭐ 31🍴，`/skills/self-improvement/` 是 skill 源）
- ✅ **官方手动安装命令**：`git clone https://github.com/pskoett/self-improving-agent.git ~/.workbuddy/skills/self-improving-agent`（原路径 `~/.openclaw/skills/`，WorkBuddy 换成 `~/.workbuddy/skills/`）
- ✅ **版本迭代**：v1.0（2024 初）→ v1.3（2025 初，加 remind 钩子），目录结构含 assets/hooks/openclaw/references/scripts/skill.md/readme.md

### 结论
**安总原计划"回家打包发 GitHub→明天再拉"可以省掉**，直接从 pskoett 官方仓库 clone 即可。推荐方案 C 双保险：明天早上公司 PC 先 clone 官方版启用，晚上安总对比家里版本有差异再覆盖。

### 待安总定方案
方案 A（明天 5 分钟 clone 官方）/ 方案 B（等打包）/ 方案 C（双保险，官方先装+回家对比）

---

## 16:03 ✅ 方案 A 即刻执行：公司 PC pskoett/self-improving-agent 安装成功

### 执行命令
```bash
git clone https://github.com/pskoett/self-improving-agent.git ~/.workbuddy/skills/self-improving-agent
```

### 安装结果
- 路径：`C:/Users/anguscui/.workbuddy/skills/self-improving-agent/`
- 目录结构：`SKILL.md (21KB) + README.md + assets/ + hooks/openclaw/ + references/ (examples.md / hooks-setup.md / openclaw-integration.md) + scripts/ (activator.sh / error-detector.sh / extract-skill.sh) + .git/`

### 正版铁证（装完验证）
- ✅ **name**: `self-improvement`（与聚合仓 `pskoett-ai-skills/skills/self-improvement/` 对应）
- ✅ **description**: 六大触发场景（命令失败/用户纠正/能力缺失/API失败/知识过时/更优方案）—— 与五一记录"错题本三件套"100% 吻合
- ✅ **三件套机制**: `.learnings/{LEARNINGS.md, ERRORS.md, FEATURE_REQUESTS.md}`
- ✅ **安全性声明**: SKILL.md 明确写 "Do not log secrets, tokens, private keys, environment variables" —— 与 capability-evolver 后门形成鲜明对比

### 公司 PC 必装 skill 进度更新
| # | Skill | 状态 |
|---|---|---|
| 1 | bootstrap | ✅ 已装 |
| 2 | memory-auto-sync | ✅ 已装 |
| 3 | **self-improving-agent（pskoett）** | ✅ **16:03 新装** |
| 4 | Angus密码箱 | ✅ 已装 |
| 5 | 汇报文档撰写 | ✅ 已装 |
| 6 | obsidian | ⏳ 待 GUI 手动装 |
| 7 | agent-browser | ⏳ 待 GUI 手动装 |
| 8 | docx | ⏳ 待 GUI 手动装 |
| 9 | pdf | ⏳ 待 GUI 手动装 |
| 10 | pptx | ⏳ 待 GUI 手动装 |
| 11 | xlsx | ⏳ 待 GUI 手动装 |

剩 6 个必装（都是 WorkBuddy 市场套件）需安总在 GUI「技能→添加技能」手动搜装。

### 关键新知
- 📌 **Git clone 手动装 skill 到 `~/.workbuddy/skills/<name>/` 是有效路径**，WorkBuddy 可识别（需重启/下次会话验证）
- 📌 **原计划绕一圈"回家打包→推 GitHub→明天拉"可省**——官方 fork 源码 10⭐ 21 commits 已完全可用
- 📌 **装 skill 后别忘了核查 SKILL.md 里的 name/description 字段**，确认不是同名假货

---

## 16:15-16:22 Obsidian 知识库四件套公司 PC 搭建（复刻家里 2026-05-04 配置）

### 截图核对（16:10 安总发）
公司 PC WorkBuddy 已装 skill = **16 个**，11 必装只剩 obsidian。截图列表：腾讯文档 / Angus密码箱 / bootstrap / memory-auto-sync / **self-improvement**（✅ 16:03 新装生效） / tencent-docs / 汇报文档撰写 / agent-browser / playwright-cli / find-skills / document-skills / finance-data / pptx / pdf / docx / xlsx。

### 四件套任务
1. steipete/obsidian skill（89K 下载/366 星）—— **家里记录数据错了**，实际 273⭐ 2.1k下载（remoteopenclaw.com 2026-02-07），但**仓库名 steipete/obsidian 正确**
2. Obsidian v1.12.7 桌面端（295MB）手动装
3. GitHub 私有仓库 `anguscwf/obsidian-vault`（家里已建）
4. Obsidian Git 插件 v2.32.1（家里从 release 手装）
5. 每 10 分钟 pull+push 自动同步

### 关键发现：家里 vault 已完整同步到 GitHub，省一大半事
```bash
cd ~/obsidian-vault && git fetch origin
# 拉到家里 main 分支，包含：
# - .obsidian/ 全部配置（app/appearance/core-plugins/workspace）
# - .obsidian/plugins/obsidian-git/ 插件二进制（main.js/manifest.json/styles.css）
# - .obsidian/obsidian-git-data.json（autoPullInterval:10 / pullBeforePush:true / autoPullOnBoot:true）
# - 目录结构含 .gitkeep 占位符
# - 一条测试笔记 2026-05-05测试笔记.md
# 家里 commits：9d63b26→4611842→cada312→8e2f0e6→fe6abf3（HEAD）
```
**→ 意味着：Obsidian Git 插件 v2.32.1 不用再从 release 下载**，只要 Obsidian 桌面端装好、打开 vault 目录即可自动加载插件。

### AI 能自动化做完的部分 ✅
1. ✅ 创建 vault 骨架 `~/obsidian-vault/{每日笔记,项目笔记/{NGL,BDO汉化,赫菲积分},技术文档,汇报素材}`
2. ✅ git init + 配 remote `git@github.com:anguscwf/obsidian-vault.git`
3. ✅ `git fetch origin` + `git reset --hard origin/main` 拉齐家里全部配置和插件
4. ✅ 下载 Obsidian-1.12.7.exe 到桌面（`C:/Users/anguscui/Desktop/Obsidian-1.12.7.exe` 282MiB=295MB）

### AI 做不了、安总必须手动的部分 🚫
1. 🚫 **跑 Obsidian-1.12.7.exe 安装桌面端**（295MB 交互式 .exe，GUI 点下一步）
2. 🚫 **Obsidian 打开 vault**：启动后选"打开已有文件夹" → `C:\Users\anguscui\obsidian-vault`，会提示"是否信任作者并启用插件"→**点"Trust author and enable plugins"**（否则 Git 插件不会加载）
3. 🚫 **WorkBuddy GUI 装 steipete/obsidian skill**（该 skill 没公开 GitHub 源码，市场搜 `obsidian` 或 `steipete/obsidian` 添加）

### steipete/obsidian skill 源码查证失败
- ❌ `https://github.com/steipete/obsidian` → 404
- ❌ `https://github.com/steipete/skills` → 404
- ✅ 市场商店页存在：`https://openclawskills.best/skills/steipete/obsidian/`（owner: steipete, MIT, Feb 7 2026 updated）
- → **结论**：steipete/obsidian 不开放 GitHub 源码 clone，只能走 WorkBuddy GUI 市场安装（或 clawhub CLI `openclaw add @steipete/obsidian`）

### 教训沉淀
- 📌 **不是所有市场 skill 都有公开 GitHub 源码**——self-improving-agent 有，steipete/obsidian 没有。遇到新 skill 先 404 探测再决定走 clone 还是 GUI
- 📌 **私有 vault 跨设备迁移可省 50% 功夫**——只要家里推了 GitHub，公司 PC `git fetch + reset --hard origin/main` 就能拿到**包括插件二进制**的全部配置，不用重新下 release zip
- 📌 **Obsidian 打开 vault 后必须 "Trust author and enable plugins"**，否则即使插件文件在 `.obsidian/plugins/` 也不会激活
- 📌 **家里记录的"89K 下载/366 星"不准**——实际 2.1k/273，以后 skill 数据类记录要写明采集日期




